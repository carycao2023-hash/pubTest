# 基于 Agent Skill 架构的企业级智能问数（ChatBI）系统详细设计方案

> 版本：v1.1（含自查修正记录，见第 11 章）
> 定位：可治理、可上线、可扩展的企业级 BI 智能问答平台

---

## 0. 设计目标与核心原则

本系统的目标是让业务用户用自然语言完成"问数（取数）、问趋势、问原因（归因）、下钻、泛问（业务知识问答）"五类任务，并在此之上提供业务洞察与决策支持。要达到企业级要求，必须同时满足三条硬约束：**答案可信（数字可溯源、口径一致）**、**数据安全（权限隔离不可绕过）**、**系统可治理（语义资产有 Owner、有版本、有审批）**。

由此确立五条贯穿全篇的核心设计原则：

**原则一：LLM 不直接写物理 SQL。** LLM 只负责把自然语言翻译成受约束的"语义查询计划（Query DSL）"，由确定性的语义层编译器将 DSL 编译为 SQL。这是可信问数与权限隔离的技术基石——所有口径逻辑、Join 关系、行级权限过滤都在编译期由代码（而非模型）注入，模型幻觉的影响面被压缩到"选错指标/维度"这一可校验的范围内。

**原则二：语义层是唯一事实来源（Single Source of Truth）。** 指标口径、维度定义、同义词、计算逻辑全部沉淀在可独立编辑、有版本管理的语义层中，问数结果与既有 BI 看板同源，避免"AI 说一个数、看板说另一个数"。

**原则三：权限在编译期与检索期双重生效。** 不仅查询结果要过滤，连注入给 LLM 的元数据（指标、维度、样例值）也必须先按用户权限裁剪，防止通过提示词泄露无权资产的存在性信息。

**原则四：算法计算、LLM 解读。** 归因、异动检测、趋势分解等分析由确定性统计算法完成，LLM 只负责将算法结果翻译为业务语言，不允许 LLM"看着数据编结论"。

**原则五：不确定就澄清，不猜测。** 意图或槽位置信度不足时，主动向用户澄清（并给出候选项），而不是给出一个可能错误的数字。宁可多问一句，不可错答一个数。

---

## 1. 总体架构

### 1.1 分层架构图

```
┌────────────────────────────────────────────────────────────────────┐
│  接入层  Web / 移动端 / 飞书・钉钉・企微机器人 / 嵌入式SDK / OpenAPI     │
├────────────────────────────────────────────────────────────────────┤
│  网关层  统一认证(SSO/OIDC) · 会话管理 · 限流 · 审计埋点               │
├────────────────────────────────────────────────────────────────────┤
│  Agent 编排层 (Orchestrator)                                        │
│   ├─ 意图理解与路由（规则快筛 + LLM 分类 + 置信度门控 + 澄清）           │
│   ├─ 多轮对话状态机（上下文继承、指代消解、槽位管理）                    │
│   └─ Skill 调度与结果聚合（支持多 Skill 串/并行编排）                   │
├────────────────────────────────────────────────────────────────────┤
│  Skill 层（可插拔注册，统一契约）                                      │
│   MetricQuery  Trend  RootCause  DrillDown  Compare                 │
│   Forecast  OpenQA(RAG)  Insight  Visualization  Report             │
├────────────────────────────────────────────────────────────────────┤
│  语义层 (Semantic Layer) —— 独立服务 + 独立编辑台                      │
│   指标/维度/口径建模 · 同义词与术语 · 语义检索 · DSL→SQL 编译器          │
│   版本管理 · 审批发布 · 血缘                                          │
├────────────────────────────────────────────────────────────────────┤
│  查询执行层                                                          │
│   计划校验器 · 权限注入(RLS/CLS) · 成本预估 · 查询路由                  │
│   结果缓存 · 限流熔断 · 只读执行                                      │
├────────────────────────────────────────────────────────────────────┤
│  数据层  OLAP引擎(StarRocks/Doris/ClickHouse/Trino) · 数仓(ODS→ADS)   │
│         元数据库(PostgreSQL) · 向量库 · Redis缓存 · 对象存储           │
├────────────────────────────────────────────────────────────────────┤
│  平台横切能力                                                        │
│   LLM网关(多模型/降级/成本) · 可观测(Trace/日志/成本) · 权限中心          │
│   评测与回归平台 · 反馈闭环 · 审计合规                                 │
└────────────────────────────────────────────────────────────────────┘
```

### 1.2 一次典型请求的端到端流程

以"华东区上个月手机品类的销售额为什么下降了？"为例：

1. **网关层**完成认证，取得用户身份与权限主体（部门=华东、角色=区域经理）。
2. **编排层**意图分类 → `root_cause`（归因），抽取槽位：指标=销售额、维度过滤=区域:华东 + 品类:手机、时间=上月 vs 上上月。
3. **语义检索**：按用户权限裁剪后，从语义层召回"销售额"指标定义（含口径说明）、可下钻维度层级（区域→省→市，品类→品牌→SKU）、同义词映射。
4. **RootCause Skill** 生成基线查询与对比查询的 Query DSL → 校验器校验 → 语义层编译为 SQL（自动注入 `region='华东'` 的行级权限条件）→ 执行。
5. **归因算法**（贡献度分解 + 异动检测）在各候选维度上计算贡献度，找出 Top 驱动因子（如"品牌 A 销量下滑贡献了 62% 的降幅"）。
6. **LLM 解读**：仅基于算法输出的结构化结论生成业务化表述，附带口径卡片（指标定义、数据截止时间、SQL、置信说明）。
7. **审计**：全链路 trace（意图、DSL、SQL、耗时、token 成本）落审计日志。

### 1.3 Skill 统一契约

所有 Skill 实现同一接口，保证可插拔与可编排：

```python
class SkillContext:
    user: UserPrincipal          # 用户身份与权限指纹
    session: DialogueState       # 多轮状态（上一查询的DSL、澄清中的槽位）
    semantic_assets: AssetBundle # 已按权限裁剪的语义资产召回结果
    budget: Budget               # token/查询成本预算与超时配置

class Skill(Protocol):
    name: str
    intents: list[str]                          # 声明可处理的意图
    def plan(self, query, ctx) -> QueryPlanDSL | Clarification
    def execute(self, plan, ctx) -> SkillResult # 内部走统一查询服务
    def explain(self, result, ctx) -> Answer    # 生成回答+溯源卡片
```

Skill 注册采用声明式清单（名称、意图、所需权限、依赖的语义资产类型、版本），新增分析能力（如"预测"）只需实现契约并注册，编排层无需改代码——这是"可扩展"的落点。

---

## 2. 意图理解与 Agent 编排

### 2.1 意图体系

| 意图 | 示例问法 | 路由目标 Skill |
|---|---|---|
| 问数 metric_query | "上个月华东销售额是多少" | MetricQuery |
| 对比 compare | "华东和华南 Q2 的毛利对比" | Compare |
| 趋势 trend | "近半年 DAU 走势如何" | Trend |
| 归因 root_cause | "为什么 GMV 环比跌了" | RootCause |
| 下钻 drill_down | "按品类拆一下 / 再往下看省份" | DrillDown |
| 预测 forecast | "预计本季度能完成目标吗" | Forecast |
| 泛问 open_qa | "销售额和 GMV 口径有什么区别" | OpenQA(RAG) |
| 闲聊/越界 out_of_scope | "帮我写周报" | 拒答/引导 |

### 2.2 三级路由与置信度门控

采用"便宜的先跑"的级联策略控制延迟与成本：

1. **规则快筛（<5ms）**：正则与关键词命中高置信模式（如"为什么/原因" → 归因候选；"走势/趋势" → 趋势候选），同时拦截明显越界请求。
2. **LLM 分类 + 槽位抽取（一次调用合并完成）**：输出意图、置信度、槽位（指标、维度、过滤、时间、粒度）。使用 function calling / JSON Schema 强约束输出结构。
3. **置信度门控**：`conf ≥ 0.85` 直接路由；`0.6 ≤ conf < 0.85` 执行但在回答中标注理解并提供"不是想问这个？"的一键纠正；`conf < 0.6` 触发澄清对话，给出 2-4 个候选理解供点选。

阈值不是拍脑袋值，由评测集上的"错答代价曲线"标定，并按意图类型差异化（问数类错答代价高，阈值更严；泛问类可放宽）。

### 2.3 多轮对话状态机

多轮是 ChatBI 体验的分水岭。维护一个显式的 `DialogueState`：

```json
{
  "last_plan": { "metric": "sales_amount", "dims": ["region"],
                 "filters": [{"field":"region","op":"=","value":"华东"}],
                 "time": {"grain":"month","range":"2026-07"} },
  "pending_clarification": null,
  "entity_memory": {"它":"sales_amount", "这个区域":"华东"}
}
```

处理三类典型承接：**槽位继承**（"那华南呢？" → 复制 last_plan 仅替换 region）、**下钻承接**（"按品类拆一下" → 在 last_plan 上追加 group by 维度）、**指代消解**（"它上个月的环比" → entity_memory 解析）。状态随会话持久化于 Redis，TTL 与会话一致；每次继承后生成"本次理解"的自然语言回显（如"（继续看华南地区 2026 年 7 月）"），让用户可即时纠错。

---

## 3. 语义层设计（核心，独立可编辑）

语义层是本系统的心脏，承担四个职责：(a) 用业务语言而非表结构描述数据；(b) 沉淀口径与特殊计算逻辑；(c) 为 LLM 提供可检索、可注入的语义上下文；(d) 将 Query DSL 确定性地编译为 SQL。它以独立微服务 + 独立编辑台的形式存在，可脱离问数前端单独治理和复用（既有 BI 看板也可接同一语义层取数）。

### 3.1 建模对象与元模型

```
数据源 DataSource ─┬─ 逻辑模型 Model（映射物理表/视图，声明主键、Join关系、时间列）
                   ├─ 维度 Dimension（类型：枚举/时间/层级；含层级路径、样例值、同义词）
                   ├─ 指标 Metric
                   │    ├─ 原子指标（列 + 聚合函数 + 过滤，如 SUM(amount) WHERE status='paid'）
                   │    ├─ 派生指标（原子指标 + 时间/维度修饰，如 销售额_华东_本月）
                   │    └─ 复合指标（表达式引用其他指标，如 毛利率 = (收入-成本)/收入）
                   ├─ 计算模板 CalcTemplate（YoY/MoM/移动平均/累计/去重口径等可复用逻辑）
                   ├─ 术语表 Glossary（业务黑话、缩写、实体别名："双十一"→时间范围）
                   └─ 默认约定 Defaults（默认时间范围、默认过滤、数值格式、单位）
```

### 3.2 语义定义 DSL（YAML，可导入导出、可 Git 化）

用户要求的三项能力——同一字段的多种表达、字段详细解释、特殊计算逻辑——分别对应 `synonyms`、`description`（区分给人看与给 LLM 看的版本）、`expr/calc_templates` 字段：

```yaml
metric:
  name: sales_amount
  display_name: 销售额
  synonyms: [销售金额, 营收, 销售收入, GMV口径销售额, sales, revenue]
  description: >-                      # 给业务用户看的解释
    已支付订单的商品实付金额合计，不含运费与退款，单位：元。
  llm_hint: >-                         # 给 LLM 的消歧提示（注入 prompt）
    注意与 GMV 区分：GMV 含未支付与退款订单。用户说"营收"时默认指本指标；
    若上下文出现"含退款"，应改用 gmv 指标并向用户确认。
  model: dwd_order_detail
  expr: SUM(pay_amount)
  filters: [ "order_status = 'paid'", "is_refund = 0" ]
  unit: 元
  format: "#,##0.00"
  default_time_dim: pay_date
  owner: zhangsan@corp.com             # 治理：口径责任人
  version: 3
  status: published                    # draft / in_review / published / deprecated
  certified: true                      # 认证指标，问数优先命中

dimension:
  name: region
  display_name: 区域
  synonyms: [大区, 地区, 区域名称]
  hierarchy: [region, province, city]  # 下钻路径
  sample_values: [华东, 华南, 华北]     # 用于值消歧与LLM few-shot
  value_mapping: { "江浙沪": ["上海","江苏","浙江"] }   # 业务黑话→值集合

calc_template:
  name: yoy                            # 特殊计算逻辑的可复用模板
  display_name: 同比
  synonyms: [同比增长, 比去年同期]
  expr: "( {metric} - {metric}<time_shift: -1y> ) / ABS({metric}<time_shift: -1y>)"
  format: "0.00%"

glossary:
  - term: 双十一
    resolve_to: { type: time_range, value: ["{year}-11-01", "{year}-11-11"] }
  - term: 大盘
    resolve_to: { type: filter, value: "全渠道无过滤" }
```

复合指标与"汇总分析类字段的特殊计算逻辑"示例：

```yaml
metric:
  name: gross_margin_rate
  display_name: 毛利率
  type: composite
  expr: "SAFE_DIVIDE( {revenue} - {cost}, {revenue} )"   # 编译器展开为子查询/CTE
  non_additive: true        # 声明不可加：禁止对毛利率直接 SUM，跨维度汇总时
                            # 必须先汇总分子分母再相除（编译器强制执行）
metric:
  name: active_customer_cnt
  display_name: 活跃客户数
  expr: COUNT(DISTINCT customer_id)
  non_additive: true        # 去重指标不可跨分区直接相加，禁止基于日表求和月值
  reaggregation: from_detail  # 汇总口径：必须回明细重算 distinct
```

`non_additive` / `reaggregation` 这类声明是"可信问数"的关键细节：比率与去重指标的错误汇总是 ChatBI 最常见的口径事故，必须由编译器基于声明强制拦截，而不是指望 LLM 记住。

### 3.3 语义编辑台（独立产品模块）

面向数据治理团队与业务分析师，核心功能：

1. **可视化建模**：拖拽绑定物理表 → 定义模型 Join → 配置指标/维度表单（同义词、解释、口径、格式均为独立可编辑字段）；同时支持 YAML 源码模式与批量导入导出（Git 双向同步，支持 CI 校验）。
2. **版本与审批**：每个语义对象独立版本号；变更走 `draft → in_review → published` 审批流，口径变更需 Owner 审批；已发布版本支持灰度（先对某部门生效）与一键回滚。发布后自动失效相关查询缓存。
3. **调试与预览**：编辑台内置"用这个指标试问一句"的沙箱，实时预览 DSL、编译 SQL 与结果，所见即所得地验证同义词与口径是否生效。
4. **冷启动辅助**：从数仓元数据（列名、注释、血缘）+ 存量看板 SQL 自动生成语义草稿，人工确认后发布，降低初始建模成本；同义词可由 badcase 挖掘持续推荐补充。
5. **健康度看板**：无 Owner 指标、同义词冲突（同一别名指向多个指标）、长期未被命中的僵尸资产、口径重复度检测。同义词冲突在保存时强校验：同一别名在同一主题域内只能指向一个已发布对象，跨主题域冲突则要求配置消歧优先级。

### 3.4 语义资产如何供给 LLM（检索式注入，而非全量注入）

企业级语义层动辄数千指标，全量注入 prompt 既超上下文又引入噪声。采用两阶段供给：

1. **离线索引**：对每个已发布语义对象构建检索文档（display_name + synonyms + description + llm_hint + 样例值），双路索引：BM25 关键词 + 向量嵌入。
2. **在线召回**：用户提问 → 先按用户权限过滤候选资产集 → 双路召回 Top-K（指标 K≈5、维度 K≈8）→ 轻量重排 → 组装为紧凑的结构化上下文注入 LLM。召回结果同时附带"认证标记"，LLM 被指示优先选择 certified 资产，多个候选难以区分时向用户澄清（"你说的营收是指【销售额】还是【GMV】？"）。

---

## 4. 查询计划生成、校验与执行

### 4.1 Query DSL：LLM 的唯一出口

LLM 通过 function calling 输出符合 JSON Schema 的查询计划，Schema 中指标/维度字段用枚举约束为"本次召回且用户有权的资产 ID"，从结构上杜绝编造字段：

```json
{
  "intent": "metric_query",
  "metrics": ["sales_amount"],
  "calc": [{ "template": "yoy", "on": "sales_amount" }],
  "group_by": ["region"],
  "filters": [
    { "dim": "category", "op": "=", "value": "手机" },
    { "dim": "pay_date", "op": "range", "value": ["2026-07-01", "2026-07-31"] }
  ],
  "order_by": [{ "field": "sales_amount", "dir": "desc" }],
  "limit": 50,
  "confidence": 0.93,
  "assumptions": ["'上个月'解析为2026-07；用户未指定渠道，默认全渠道"]
}
```

`assumptions` 字段强制 LLM 显式声明其默认假设，原样回显给用户——这是"可信"的低成本高收益设计。

### 4.2 四道确定性校验闸门

1. **Schema 校验**：JSON 结构、枚举合法性、类型正确性。失败则带错误信息让 LLM 重试（最多 2 次），仍失败转澄清。
2. **语义校验**：指标与维度是否可组合（基于模型 Join 图可达性）；non_additive 指标的聚合方式是否合法；时间粒度与指标默认时间维是否匹配；维度值是否存在（对枚举维做值校验，"手机"不在品类值域时做模糊匹配并回显确认）。
3. **权限校验**：指标级/维度级/行级权限复核（详见第 6 章），无权资产在召回期已被过滤，此处为纵深防御的第二道。
4. **成本校验**：基于统计信息预估扫描量与结果行数；超阈值则要求收窄时间范围或维度基数（"该查询将扫描 2 年明细约 40 亿行，建议缩小到近 3 个月"），高基数维度 group by 强制 TopN + 其他。

### 4.3 编译与执行

编译器将 DSL 确定性地翻译为 SQL：解析指标表达式 → 展开复合指标为 CTE → 根据 Join 图生成关联 → 注入口径过滤与行级权限谓词 → 应用计算模板（时间平移用窗口或自 Join，按引擎方言生成）→ 输出参数化 SQL。

执行侧要点：**只读专用账号**（数据库层面兜底，即使全链路被绕过也无法写操作）；**查询路由**（小查询走 OLAP 聚合表，明细重算走明细层，可配置多引擎）；**双层缓存**（结果缓存 key = DSL 规范化哈希 + 权限指纹 + 语义层版本 + 数据分区水位，任一变化即失效；元数据缓存加速编译）；**限流熔断**（按用户/部门配额，超时 30s 熔断并提示异步取数通道——大查询转异步任务，完成后消息通知）。

### 4.4 结果后处理

数值按语义层声明的单位/格式渲染；根据结果形态推荐图表（单值→指标卡，时间序列→折线，维度对比→柱状，占比→饼/条形）；生成一段"数据解读"文本时，LLM 只允许引用结果集中真实存在的数字（后置正则校验回答中的数字必须能在结果集或其简单换算中找到，防止解读环节幻觉）。

---

## 5. 可信问数机制（Trust by Design）

可信不是一个功能，而是一组贯穿链路的机制的总和：

**1）口径唯一与看板同源。** 问数与看板走同一语义层同一版本，物理上消除"两个数"。语义层版本号随答案返回，可追查历史任意时点的口径。

**2）全程可溯源。** 每个数字附带可展开的"溯源卡片"：指标定义与口径说明、命中的语义资产版本、时间与过滤条件的解析结果、生成的 SQL（可复制供数据团队复核）、数据更新水位（"数据截至 2026-08-18，T+1 更新"）、本次回答的 assumptions。

**3）确定性优先。** SQL 由编译器生成而非 LLM；归因结论由算法计算而非 LLM 观察；LLM 的自由发挥被限制在意图理解与文字表述两端，且都有校验或回显兜底。

**4）分级置信呈现。** 高置信直接给数；中置信给数并显著标注理解假设；低置信不给数，转澄清。回答模板明确区分"事实（查询结果）/ 推断（归因结论及其解释力）/ 建议（决策参考）"三种语气，决策建议一律标注"供参考，请结合业务判断"。

**5）评测与回归。** 建设分层评测集：意图分类集（≥500 条标注问句）、NL→DSL 集（问句与标准 DSL 对）、端到端数值集（问句与标准答案数值，基于固定测试数据快照）。每次语义层发布、prompt 变更、模型升级都跑回归，准确率低于基线阻断上线。线上抽样人工复核 + 用户反馈（踩）自动进 badcase 队列。

**6）失败的诚实。** 查不到、无权、超成本、低置信，都明确说明原因与建议动作，绝不返回编造的数字。空结果时主动做归因提示（"该条件下无数据，可能因为：品类名应为'手机通讯'/该月数据尚未产出"）。

---

## 6. 权限与数据访问隔离

### 6.1 权限模型

对接企业统一身份（SSO / OIDC / LDAP），权限中心维护四层权限，全部以"策略"形式存储并可审计：

| 层级 | 控制内容 | 实现方式 |
|---|---|---|
| 功能权限 | 能否使用问数/归因/导出等能力 | RBAC，网关与编排层校验 |
| 资产权限 | 可见哪些主题域/模型/指标/维度 | 资产 ACL，作用于语义检索与校验闸门 |
| 行级权限 RLS | 同一指标只能看自己范围的数据 | 权限谓词在 SQL 编译期强制注入 |
| 列级权限 CLS | 敏感字段脱敏或禁查（手机号、成本价） | 编译期替换为脱敏表达式或拒绝 |

RLS 策略示例（策略与语义层解耦，独立管理、复用企业既有数据权限系统的判定结果）：

```yaml
rls_policy:
  model: dwd_order_detail
  rules:
    - principal: role:regional_manager
      predicate: "region IN ({user.regions})"      # 谓词变量来自权限中心实时解析
    - principal: role:hq_analyst
      predicate: "1=1"
  default: deny                                     # 未命中策略默认拒绝
```

### 6.2 三个必须堵住的旁路

企业级方案与 Demo 的差距主要在旁路封堵：

**旁路 1：提示词泄露。** 无权资产的名称、描述、样例值绝不能进入该用户的 LLM 上下文（否则用户可套出"利润率"指标的存在与口径）。因此权限过滤发生在语义召回之前，而非之后。

**旁路 2：缓存穿透。** 结果缓存 key 必须包含权限指纹（用户权限策略集合的哈希）。华东经理与华南经理问同一句话，是两个缓存条目；权限变更时权限指纹变化，旧缓存自然失效。

**旁路 3：聚合推断与差分。** 行级权限用户通过"全国总额 − 自己区域"反推他区数据。缓解手段：无权维度不允许出现在 group_by 与 filter 中（校验闸门拦截）；对声明为敏感的指标可配置最小聚合粒度（结果行的底层实体数低于 N 则抑制显示）。此项按数据敏感级别选择性开启，避免过度影响可用性。

### 6.3 审计

全量记录：用户、原始问句、意图、DSL、编译 SQL、命中权限策略、返回行数、导出行为。审计日志不可篡改（追加式存储），支持按用户/指标/时间检索，满足数据合规审查（如个人信息保护与内控要求）。

---

## 7. 分析类 Skill 详细设计

### 7.1 Trend（趋势）

生成时间序列 DSL（自动选择合理粒度：跨度>2年→月，>2月→周，否则→日）；算法侧做 STL 分解（趋势/季节/残差）与变点检测（如 PELT）、异常点标记（残差超 3σ）；LLM 基于分解结果输出"整体趋势 + 季节规律 + 异常点及其日期"的解读。

### 7.2 DrillDown（下钻）

依托语义层维度层级。两种触发：显式（"按品类拆"）与承接式（"再往下看"→ 取当前维度层级的下一级）。防爆炸控制：下钻结果默认 TopN(20) + "其他"合并；多维交叉下钻限制最多 2 个维度同时展开；超基数时提示先过滤。

### 7.3 RootCause（归因）——算法计算、LLM 解读

流程：确认异动（对比期 vs 基期的变化是否显著）→ 候选维度圈定（语义层中标注了 `attributable: true` 的维度，避免在无业务意义的维度上归因）→ 维度贡献度分解：

- **可加指标**（销售额、订单量）：各维度成员贡献度 = Δ成员值 / Δ总值，按贡献度与解释力排序；多维时逐维打分选 Top 维度再交叉。
- **比率指标**（转化率、毛利率）：双因素分解（分子分母各自贡献）+ 加权结构分解，区分"率本身变了"与"结构变了"（Simpson 悖论场景必须识别并明示）。
- 可选增强：Adtributor / 基尼系数法用于多维根因定位。

输出结构化归因结果（因子、贡献度、方向、显著性），LLM 仅将其转述为业务语言，并明确说明"贡献度合计解释了变化的 X%，剩余部分未被现有维度解释"。归因给出的是相关性分解而非因果结论，回答模板中固定注明。

### 7.4 OpenQA（泛问，RAG）

回答口径解释、业务知识、系统用法三类问题。知识库来源：语义层的指标/术语描述（结构化，最高优先级）、企业上传的业务文档（Wiki、制度文件，需带权限标签）。严格 RAG：无召回支撑则拒答并引导（"这个问题我在知识库中没有依据，建议咨询数据团队"），禁止用模型通用知识回答企业内部事实。文档权限与数据权限统一由权限中心管理。

### 7.5 Insight（洞察）与决策支持

两种形态：**被动洞察**——每次问数结果附带轻量洞察（环比异动、贡献集中度、达标进度），由规则引擎从结果集计算触发，成本近乎为零；**主动洞察**——订阅式巡检任务（每日扫描核心指标异动，触发归因并推送报告）。决策支持 = 归因结论 + 语义层中沉淀的"指标运营知识"（如指标 Owner 配置的常见波动原因与应对动作）+ LLM 组织成建议，全部标注为参考性质，不做自动化决策执行。

---

## 8. 工程架构与技术选型

### 8.1 服务拆分（微服务，K8s 部署）

| 服务 | 职责 | 关键技术 |
|---|---|---|
| bi-gateway | 认证、限流、审计埋点、WebSocket 流式输出 | Kong/APISIX 或自研 |
| agent-orchestrator | 意图路由、对话状态、Skill 编排 | Python(LangGraph 或自研状态机) |
| skill-services | 各分析 Skill（可独立扩缩容） | Python，统计库 statsmodels/ruptures |
| semantic-service | 语义 CRUD、检索、DSL 编译、版本审批 | Java/Go + PostgreSQL；编译器自研或基于 Cube/dbt SL 改造 |
| query-service | 校验、权限注入、路由执行、缓存 | Go；连接池管理多引擎 |
| permission-service | 策略管理、RLS 谓词解析、权限指纹 | 对接企业 IAM |
| llm-gateway | 多模型接入、降级、重试、成本核算、内容审计 | 支持私有化模型与云 API 双轨 |
| eval-platform | 评测集管理、回归执行、badcase 流转 | 离线任务 |

### 8.2 关键选型考量

- **语义层**：若企业已有 dbt 体系可基于 dbt Semantic Layer/MetricFlow 扩展；需要强多引擎与缓存能力可选 Cube；对同义词/llm_hint/中文治理流程要求高时建议"开源编译内核 + 自研元模型与编辑台"。
- **OLAP**：国内生态推荐 StarRocks/Doris（高并发聚合 + 明细查询均衡）；已有 ClickHouse/Trino 则通过 query-service 方言层适配。
- **LLM**：意图分类与槽位抽取可用较小模型（成本敏感、高频），DSL 生成与解读用旗舰模型；金融等强合规场景支持私有化部署模型，llm-gateway 屏蔽差异。数据结果默认不出域：解读环节仅传聚合结果的必要摘要，且可配置脱敏。
- **可观测**：OpenTelemetry 全链路 trace（一次问答的意图/召回/编译/执行/解读各段延迟与 token 成本可视），核心 SLO：P95 端到端 ≤ 8s（问数类）/ ≤ 20s（归因类），意图准确率 ≥ 95%，DSL 可执行率 ≥ 98%。

### 8.3 多租户与扩展性

语义资产、权限策略、缓存、审计全部带 tenant_id 隔离；Skill、语义对象、计算模板、图表类型均为注册制扩展点；对外提供 OpenAPI（问数即服务），供下游系统嵌入。

---

## 9. 治理与运营闭环

**语义治理**：每个指标必须有 Owner；口径变更审批 + 变更通知订阅者；季度性资产盘点（僵尸指标下线、重复口径合并）；"认证指标"体系让高质量资产优先命中。

**质量运营**：badcase 闭环——用户踩/人工抽检 → 归类（意图错/资产缺同义词/口径缺失/编译 bug）→ 派发（补同义词进语义层、补评测集、修 prompt/代码）→ 回归验证。上线初期配置"数据管家"角色每日复核抽样问答。

**运营指标看板**：周活问数用户、问答量、意图分布、准确率（抽检）、澄清率、拒答率、P95 延迟、单次问答成本、Top 未命中问句（指导语义层建设优先级）。

---

## 10. 分阶段落地路线

| 阶段 | 周期 | 范围 | 出口标准 |
|---|---|---|---|
| P0 试点 | 6-8 周 | 1 个高价值主题域（如销售）；MetricQuery + Trend + 下钻；语义编辑台最小集（建模/同义词/审批）；RLS + 审计 | 评测集问数准确率 ≥ 90%，试点部门周活 ≥ 30% |
| P1 扩展 | 8-10 周 | RootCause + Compare + OpenQA；多轮对话完善；缓存与异步取数；评测平台 | 3 个主题域，归因结论抽检可用率 ≥ 80% |
| P2 平台化 | 持续 | 主动洞察与订阅、Forecast、多租户、OpenAPI、冷启动自动建模 | 全司推广，语义资产健康度达标 |

先做窄而深（一个域做到可信），再横向复制——ChatBI 项目最常见的死法是第一期铺 10 个域导致处处不准、信任崩塌。

---

## 11. 严谨自查与修正记录（第一轮设计评审）

以下为对初版设计做系统性检查后发现的问题与修正，均已合并进上文方案：

| # | 初版设计缺陷 | 风险 | 修正（已并入） |
|---|---|---|---|
| 1 | LLM 直接生成 SQL（NL2SQL 直连） | 幻觉字段、口径漂移、无法可靠注入行级权限、SQL 注入面大 | 改为 NL→受约束 DSL→编译器生成 SQL（§4.1-4.3），LLM 输出用枚举 Schema 限死在有权资产内 |
| 2 | 权限只在查询执行时过滤结果 | 无权指标的名称/口径经语义召回进入 prompt，存在元数据泄露 | 权限前置到语义检索之前（§3.4、§6.2 旁路1），执行期校验降为纵深防御第二道 |
| 3 | 语义资产全量注入 prompt | 数千指标超上下文窗口、噪声导致选错指标 | 改为权限裁剪后的双路检索 Top-K 注入（§3.4） |
| 4 | 归因由 LLM 直接观察数据表得出结论 | 结论不可复现、易编造因果 | 改为确定性贡献度分解算法 + LLM 仅做转述（§7.3），并区分可加/比率指标两套分解、明示 Simpson 悖论 |
| 5 | 比率/去重指标未做汇总保护 | "各日活跃用户数求和当月活"类经典口径事故 | 语义层增加 non_additive / reaggregation 声明，编译器强制拦截（§3.2） |
| 6 | 结果缓存 key 仅含查询内容 | 不同权限用户命中同一缓存造成越权 | 缓存 key 加入权限指纹 + 语义层版本 + 数据水位（§4.3、§6.2 旁路2） |
| 7 | 未处理聚合差分推断 | RLS 用户可用"总量−自己"反推他区敏感数据 | 无权维度禁止进入 group_by/filter；敏感指标可配最小聚合粒度（§6.2 旁路3） |
| 8 | 多轮对话仅靠把历史消息塞给 LLM | 指代与承接不稳定、成本高 | 显式 DialogueState 状态机 + 槽位继承规则 + 理解回显（§2.3） |
| 9 | 澄清机制缺失，低置信也硬答 | 错误数字破坏信任，且错误不可见 | 置信度门控三段策略 + assumptions 强制回显（§2.2、§4.1） |
| 10 | 语义层发布后旧缓存与旧索引仍生效 | 口径已改但答案仍用旧口径 | 发布钩子：失效结果缓存、重建检索索引、版本号进溯源卡片（§3.3、§5） |
| 11 | 高基数维度下钻/归因无防护 | 单次查询扫爆引擎、结果不可读 | 成本闸门 + TopN 合并 + 交叉维度上限（§4.2、§7.2） |
| 12 | LLM 解读环节可能编造结果集里没有的数字 | 查询对了、解读错了，且难以发现 | 解读文本后置数字校验，必须可在结果集中溯源（§4.4） |
| 13 | 空结果/失败直接返回"无数据" | 用户无法自助排错，体验差 | 失败诚实化：给原因假设与建议动作（§5-6） |
| 14 | 评测只在上线前做一次 | 模型升级/语义变更引入回归无人发现 | 三层评测集 + 变更触发式回归 + 线上抽检闭环（§5、§9） |

### 可行性复核结论

对修正后的方案按"可治理、可上线、可扩展"三项要求复核：

- **可治理**：语义资产有 Owner/版本/审批/健康度；口径变更可追溯可回滚；全链路审计。✅
- **可上线**：无未验证的技术假设——DSL 编译语义层（Cube/MetricFlow 已验证路线）、意图分类与受约束生成（成熟 LLM 能力）、归因算法（经典统计方法）均有工业落地先例；给出了 SLO、降级（澄清/拒答/异步取数）与分阶段路线。✅
- **可扩展**：Skill 注册制、语义对象与计算模板扩展点、多引擎方言层、多租户、OpenAPI。✅

### 遗留风险与边界（如实声明）

1. **复杂多事实表查询**（跨主题域大宽查询、非等值 Join）仍是语义编译的难点，P0/P1 阶段应将此类问题路由到"转人工/转专业取数工单"，不硬答。
2. **口径争议是组织问题**，技术只能暴露差异（重复口径检测）无法裁决，需配套数据治理委员会机制。
3. **归因解释力有上限**：现有维度解释不了的变化（外部事件、政策）系统必须如实说"未被解释"，可通过接入运营事件日历逐步增强。
4. **准确率无法承诺 100%**：企业推广时应明确定位为"辅助取数与分析加速器"，关键决策数字保留人工复核通道（溯源卡片即为复核入口）。
