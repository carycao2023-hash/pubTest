# 基于 Agent Skill 架构的企业级智能问数（ChatBI）系统详细设计方案

> 版本：v2.0（设计基线冻结；含三轮自查修正记录，见第 11、12、13 章；三轮修正共 36 项已全部并入前文，实施按 P0/P1/P2 排期）
> 定位：可治理、可上线、可扩展的企业级 BI 智能问答平台

---

## 0. 设计目标与核心原则

本系统的目标是让业务用户用自然语言完成"问数（取数）、问趋势、问原因（归因）、下钻、泛问（业务知识问答）"五类任务，并在此之上提供业务洞察与决策支持。要达到企业级要求，必须同时满足三条硬约束：**答案可信（数字可溯源、口径一致）**、**数据安全（权限隔离不可绕过）**、**系统可治理（语义资产有 Owner、有版本、有审批）**。

由此确立五条贯穿全篇的核心设计原则：

**原则一：LLM 不直接写物理 SQL。** LLM 只负责把自然语言翻译成受约束的"语义查询计划（Query DSL）"，由确定性的语义层编译器将 DSL 编译为 SQL。这是可信问数与权限隔离的技术基石——所有口径逻辑、Join 关系、行级权限过滤都在编译期由代码（而非模型）注入，模型幻觉的影响面被压缩到"选错指标/维度"这一可校验的范围内。

**原则二：语义层是唯一事实来源（Single Source of Truth）。** 指标口径、维度定义、同义词、计算逻辑全部沉淀在可独立编辑、有版本管理的语义层中，问数结果与既有 BI 看板同源，避免"AI 说一个数、看板说另一个数"。

**原则三：权限在编译期与检索期双重生效。** 不仅查询结果要过滤，连注入给 LLM 的元数据（指标、维度、样例值）也必须先按用户权限裁剪，防止通过提示词泄露无权资产的存在性信息。

**原则四：算法计算、LLM 解读。** 归因、异动检测、趋势分解等分析由确定性统计算法完成，LLM 只负责将算法结果翻译为业务语言，不允许 LLM"看着数据编结论"。

**原则五：不确定就澄清，不猜测。** 意图或槽位置信度不足时，主动向用户澄清（并给出候选项），而不是给出一个可能错误的数字。宁可多问一句，不可错答一个数。

---

## 1. 总体架构

### 1.1 分层架构图

```
┌────────────────────────────────────────────────────────────────────┐
│  接入层  Web / 移动端 / 飞书・钉钉・企微机器人 / 嵌入式SDK / OpenAPI     │
├────────────────────────────────────────────────────────────────────┤
│  网关层  统一认证(SSO/OIDC) · 会话管理 · 限流 · 审计埋点               │
├────────────────────────────────────────────────────────────────────┤
│  Agent 编排层 (Orchestrator)                                        │
│   ├─ 意图理解与路由（规则快筛 + LLM 分类 + 置信度门控 + 澄清）           │
│   ├─ 多轮对话状态机（上下文继承、指代消解、槽位管理）                    │
│   └─ Skill 调度与结果聚合（支持多 Skill 串/并行编排）                   │
├────────────────────────────────────────────────────────────────────┤
│  Skill 层（可插拔注册，统一契约）                                      │
│   MetricQuery  Trend  RootCause  DrillDown  Compare                 │
│   Forecast  OpenQA(RAG)  Insight  Visualization  Report             │
├────────────────────────────────────────────────────────────────────┤
│  语义层 (Semantic Layer) —— 独立服务 + 独立编辑台                      │
│   指标/维度/口径建模 · 同义词与术语 · 语义检索 · DSL→SQL 编译器          │
│   版本管理 · 审批发布 · 血缘                                          │
├────────────────────────────────────────────────────────────────────┤
│  查询执行层                                                          │
│   计划校验器 · 权限注入(RLS/CLS) · 成本预估 · 查询路由                  │
│   结果缓存 · 限流熔断 · 只读执行                                      │
├────────────────────────────────────────────────────────────────────┤
│  数据层  OLAP引擎(StarRocks/Doris/ClickHouse/Trino) · 数仓(ODS→ADS)   │
│         元数据库(PostgreSQL) · 向量库 · Redis缓存 · 对象存储           │
├────────────────────────────────────────────────────────────────────┤
│  平台横切能力                                                        │
│   LLM网关(多模型/降级/成本) · 可观测(Trace/日志/成本) · 权限中心          │
│   评测与回归平台 · 反馈闭环 · 审计合规                                 │
└────────────────────────────────────────────────────────────────────┘
```

### 1.2 一次典型请求的端到端流程

以"华东区上个月手机品类的销售额为什么下降了？"为例：

1. **网关层**完成认证，取得用户身份与权限主体（部门=华东、角色=区域经理）。
2. **编排层**意图分类 → `root_cause`（归因），抽取槽位：指标=销售额、维度过滤=区域:华东 + 品类:手机、时间=上月 vs 上上月。
3. **语义检索**：按用户权限裁剪后，从语义层召回"销售额"指标定义（含口径说明）、可下钻维度层级（区域→省→市，品类→品牌→SKU）、同义词映射。
4. **RootCause Skill** 生成基线查询与对比查询的 Query DSL → 校验器校验 → 语义层编译为 SQL（自动注入 `region='华东'` 的行级权限条件）→ 执行。
5. **归因算法**（贡献度分解 + 异动检测）在各候选维度上计算贡献度，找出 Top 驱动因子（如"品牌 A 销量下滑贡献了 62% 的降幅"）。
6. **LLM 解读**：仅基于算法输出的结构化结论生成业务化表述，附带口径卡片（指标定义、数据截止时间、SQL、置信说明）。
7. **审计**：全链路 trace（意图、DSL、SQL、耗时、token 成本）落审计日志。

### 1.3 Skill 统一契约

所有 Skill 实现同一接口，保证可插拔与可编排：

```python
class SkillContext:
    user: UserPrincipal          # 用户身份与权限指纹
    session: DialogueState       # 多轮状态（上一查询的DSL、澄清中的槽位）
    semantic_assets: AssetBundle # 已按权限裁剪的语义资产召回结果
    budget: Budget               # token/查询成本预算与超时配置

class Skill(Protocol):
    name: str
    intents: list[str]                          # 声明可处理的意图
    def plan(self, query, ctx) -> QueryPlanDSL | Clarification
    def execute(self, plan, ctx) -> SkillResult # 内部走统一查询服务
    def explain(self, result, ctx) -> Answer    # 生成回答+溯源卡片
```

Skill 注册采用声明式清单（名称、意图、所需权限、依赖的语义资产类型、版本），新增分析能力（如"预测"）只需实现契约并注册，编排层无需改代码——这是"可扩展"的落点。

**多 Skill 编排契约（OrchestrationPlan）**：多 Skill 串/并行编排需显式定义：编排 DAG（子计划间依赖与中间结果传递格式）、逐 Skill 失败隔离（单 Skill 失败时返回已成功部分并明示失败原因，不整体 fail-fast）、会话级共享的版本钉住与预算池（同一编排内所有 Skill 钉住同一语义层版本、共享总预算并按 Skill 分配）、相同子查询按规范化 DSL 哈希复用避免重复执行。多意图分解与澄清轮次上限见 §2.2。

---

## 2. 意图理解与 Agent 编排

### 2.1 意图体系

| 意图 | 示例问法 | 路由目标 Skill |
|---|---|---|
| 问数 metric_query | "上个月华东销售额是多少" | MetricQuery |
| 对比 compare | "华东和华南 Q2 的毛利对比" | Compare |
| 趋势 trend | "近半年 DAU 走势如何" | Trend |
| 归因 root_cause | "为什么 GMV 环比跌了" | RootCause |
| 下钻 drill_down | "按品类拆一下 / 再往下看省份" | DrillDown |
| 预测 forecast | "预计本季度能完成目标吗" | 引导性暂不回答（P2 前 Forecast 未实现，见 §7.6） |
| 泛问 open_qa | "销售额和 GMV 口径有什么区别" | OpenQA(RAG) |
| 闲聊/越界 out_of_scope | "帮我写周报" | 拒答/引导 |

注：**forecast 意图在 P2 前不路由到具体 Skill**，改走"引导性暂不回答"（§7.6）——明确告知能力规划、引导使用现有问数/趋势能力，不编造预测；"能否完成目标"类问题依赖语义层 PlannedMetric 目标对象（§3.1），P2 前先完成目标数据建模。

### 2.2 三级路由与置信度门控

采用"便宜的先跑"的级联策略控制延迟与成本：

1. **规则快筛（<5ms）**：正则与关键词命中高置信模式（如"为什么/原因" → 归因候选；"走势/趋势" → 趋势候选），同时拦截明显越界请求。
2. **LLM 分类 + 槽位抽取（一次调用合并完成）**：输出意图、置信度、槽位（指标、维度、过滤、时间、粒度）。使用 function calling / JSON Schema 强约束输出结构。
3. **置信度门控**（基于校准后分数，见下）：`conf ≥ 0.85` 直接路由；`0.6 ≤ conf < 0.85` 执行但在回答中标注理解并提供"不是想问这个？"的一键纠正；`conf < 0.6` 触发澄清对话，给出 2-4 个候选理解供点选。

门控不直接使用 LLM 自报置信度：自报值系统性过度自信且未校准，直接门控等于把闸门架在失真信号上。编排层将自报值作为输入特征之一，经**校准映射**转为校准后分数再门控：用评测集建可靠性图（reliability diagram）拟合"自报值→实测准确率"的映射；并辅以独立验证器（槽位与召回资产的一致性检查）与多次采样一致性作为门控信号。阈值不是拍脑袋值，由评测集上的"错答代价曲线"标定，并按意图类型差异化（问数类错答代价高，阈值更严；泛问类可放宽）。上线后持续监控校准漂移（预测置信度与实测准确率之差），模型每次升级重新标定映射与阈值。

**澄清与多意图控制**：澄清最多 **2 轮**，超限转引导或人工，避免低置信反复澄清消耗体验与成本；一句话含复合多意图（"多少？顺便对比一下"）时按意图分解为多个子计划，分别执行、按 Skill 分配预算、聚合结果——避免"一个计划扛多个意图"导致成本与部分失败语义不可控。

### 2.3 多轮对话状态机

多轮是 ChatBI 体验的分水岭。维护一个显式的 `DialogueState`：

```json
{
  "last_plan": { "metric": "sales_amount", "dims": ["region"],
                 "filters": [{"field":"region","op":"=","value":"华东"}],
                 "time": {"grain":"month","range":"2026-07"} },
  "pinned_semantic_version": "sl-2026-08",
  "pending_clarification": null,
  "entity_memory": {"它":"sales_amount", "这个区域":"华东"}
}
```

处理三类典型承接：**槽位继承**（"那华南呢？" → 复制 last_plan 仅替换 region）、**下钻承接**（"按品类拆一下" → 在 last_plan 上追加 group by 维度）、**指代消解**（"它上个月的环比" → entity_memory 解析）。状态随会话持久化于 Redis，TTL 与会话一致；每次继承后生成"本次理解"的自然语言回显（如"（继续看华南地区 2026 年 7 月）"），让用户可即时纠错。**会话开始时钉住语义层版本**（`pinned_semantic_version`）：last_plan 承接、下钻、对比均按会话开始时的版本编译，会话中途的语义层发布/灰度变更不影响进行中的会话，新版本仅对新会话生效——避免多轮中口径静默不一致（"数字在对话中悄悄变了"）且无法追查。**会话安全**：会话令牌随机生成并轮换，防固定（session fixation）；DialogueState 仅允许编排层经校验写入（entity_memory 的值必须是过闸门的资产 ID/合法值，禁止用户直接写入）；同账号多端并发会话各自独立状态，冲突时提示用户。

---

## 3. 语义层设计（核心，独立可编辑）

语义层是本系统的心脏，承担四个职责：(a) 用业务语言而非表结构描述数据；(b) 沉淀口径与特殊计算逻辑；(c) 为 LLM 提供可检索、可注入的语义上下文；(d) 将 Query DSL 确定性地编译为 SQL。它以独立微服务 + 独立编辑台的形式存在，可脱离问数前端单独治理和复用（既有 BI 看板也可接同一语义层取数）。

### 3.1 建模对象与元模型

```
数据源 DataSource ─┬─ 逻辑模型 Model（映射物理表/视图，声明主键、Join关系（含关系类型 1:1 / N:1 / N:M）、时间列）
                   ├─ 维度 Dimension（类型：枚举/时间/层级；含层级路径、样例值、同义词）
                   ├─ 指标 Metric
                   │    ├─ 原子指标（列 + 聚合函数 + 过滤，如 SUM(amount) WHERE status='paid'）
                   │    ├─ 派生指标（原子指标 + 时间/维度修饰，如 销售额_华东_本月）
                   │    └─ 复合指标（表达式引用其他指标，如 毛利率 = (收入-成本)/收入）
                   ├─ 计算模板 CalcTemplate（YoY/MoM/移动平均/累计/去重口径等可复用逻辑）
                   ├─ 目标指标 PlannedMetric（目标值、关联指标口径、发布部门、生效周期；为 Forecast 提供"目标达成对比"数据）
                   ├─ 术语表 Glossary（业务黑话、缩写、实体别名："双十一"→时间范围）
                   └─ 默认约定 Defaults（时区、历法（自然历/财务历）、周起始日、默认时间范围、默认过滤、数值格式、单位）
```

### 3.2 语义定义 DSL（YAML，可导入导出、可 Git 化）

用户要求的三项能力——同一字段的多种表达、字段详细解释、特殊计算逻辑——分别对应 `synonyms`、`description`（区分给人看与给 LLM 看的版本）、`expr/calc_templates` 字段：

```yaml
metric:
  name: sales_amount
  display_name: 销售额
  synonyms: [销售金额, 营收, 销售收入, GMV口径销售额, sales, revenue]
  description: >-                      # 给业务用户看的解释
    已支付订单的商品实付金额合计，不含运费与退款，单位：元。
  llm_hint: >-                         # 给 LLM 的消歧提示（注入 prompt）
    注意与 GMV 区分：GMV 含未支付与退款订单。用户说"营收"时默认指本指标；
    若上下文出现"含退款"，应改用 gmv 指标并向用户确认。
  model: dwd_order_detail
  expr: SUM(pay_amount)
  filters: [ "order_status = 'paid'", "is_refund = 0" ]
  unit: 元
  format: "#,##0.00"
  default_time_dim: pay_date
  sensitivity: internal                # 敏感级别标签；表达式敏感度沿依赖列传播（见 §6.1）
  owner: zhangsan@corp.com             # 治理：口径责任人
  version: 3
  status: published                    # draft / in_review / published / deprecated
  certified: true                      # 认证指标，问数优先命中

dimension:
  name: region
  display_name: 区域
  synonyms: [大区, 地区, 区域名称]
  hierarchy: [region, province, city]  # 下钻路径
  sample_values: [华东, 华南, 华北]     # 用于值消歧与LLM few-shot
  value_mapping: { "江浙沪": ["上海","江苏","浙江"] }   # 业务黑话→值集合（展开结果按 RLS 收窄，见 §6.1）

calc_template:
  name: yoy                            # 特殊计算逻辑的可复用模板
  display_name: 同比
  synonyms: [同比增长, 比去年同期]
  expr: "( {metric} - {metric}<time_shift: -1y> ) / ABS({metric}<time_shift: -1y>)"
  format: "0.00%"

glossary:
  - term: 双十一
    resolve_to: { type: time_range, value: ["{year}-11-01", "{year}-11-11"] }
  - term: 大盘
    resolve_to: { type: filter, value: "全渠道无过滤" }
```

复合指标与"汇总分析类字段的特殊计算逻辑"示例：

```yaml
metric:
  name: gross_margin_rate
  display_name: 毛利率
  type: composite
  expr: "SAFE_DIVIDE( {revenue} - {cost}, {revenue} )"   # 编译器展开为子查询/CTE
  non_additive: true        # 声明不可加：禁止对毛利率直接 SUM，跨维度汇总时
                            # 必须先汇总分子分母再相除（编译器强制执行）
metric:
  name: active_customer_cnt
  display_name: 活跃客户数
  expr: COUNT(DISTINCT customer_id)
  non_additive: true        # 去重指标不可跨分区直接相加，禁止基于日表求和月值
  reaggregation: from_detail  # 汇总口径：必须回明细重算 distinct
```

`non_additive` / `reaggregation` 这类声明是"可信问数"的关键细节：比率与去重指标的错误汇总是 ChatBI 最常见的口径事故，必须由编译器基于声明强制拦截，而不是指望 LLM 记住。

### 3.3 语义编辑台（独立产品模块）

面向数据治理团队与业务分析师，核心功能：

1. **可视化建模**：拖拽绑定物理表 → 定义模型 Join → 配置指标/维度表单（同义词、解释、口径、格式均为独立可编辑字段）；同时支持 YAML 源码模式与批量导入导出（Git 双向同步，支持 CI 校验）。
2. **版本与审批**：每个语义对象独立版本号；变更走 `draft → in_review → published` 审批流，口径变更需 Owner 审批，审批检查项包含**提示词安全审查**（description/llm_hint/sample_values 等将注入 LLM 上下文的字段不得携带指令类载荷，防间接提示词注入，见 §6.2 旁路 4）；**发布认证要求关联最少评测用例**（问句→标准 DSL/数值，见 §5），新资产不得裸奔上线。发布做**拓扑依赖校验**（复合指标不得先于其引用的原子指标发布），支持批量原子发布。已发布版本支持灰度（先对某部门生效）与一键回滚，**灰度桶标识显式进入会话状态与结果缓存 key**（§2.3、§4.3），防止灰度部门间串缓存；灰度期执行**抽样双跑影子对比**（新旧版本并行编译执行，新版本只记录不返回），差异报告超阈值自动告警/回滚；发布后自动失效相关查询缓存，检索索引重建采用**双索引别名切换**实现零停机（§3.4）。在途查询（已按旧版本编译、切换后执行）按会话钉住版本（§2.3）继续执行并在审计标记；编译/元数据缓存 key 内置语义层版本号，使失效不依赖失效广播的可靠性。
3. **调试与预览**：编辑台内置"用这个指标试问一句"的沙箱，实时预览 DSL、编译 SQL 与结果，所见即所得地验证同义词与口径是否生效。
4. **冷启动辅助**：从数仓元数据（列名、注释、血缘）+ 存量看板 SQL 自动生成语义草稿，人工确认后发布，降低初始建模成本；同义词可由 badcase 挖掘持续推荐补充。
5. **健康度看板**：无 Owner 指标、同义词冲突（同一别名指向多个指标）、长期未被命中的僵尸资产、口径重复度检测。同义词冲突在保存时强校验：同一别名在同一主题域内只能指向一个已发布对象，跨主题域冲突则要求配置消歧优先级。

### 3.4 语义资产如何供给 LLM（检索式注入，而非全量注入）

企业级语义层动辄数千指标，全量注入 prompt 既超上下文又引入噪声。采用两阶段供给：

1. **离线索引**：对每个已发布语义对象构建检索文档（display_name + synonyms + description + llm_hint + 样例值，样例值入索引前过 **PII 扫描**，见 §6.3），双路索引：BM25 关键词 + 向量嵌入；**嵌入模型版本纳入索引元数据**，升级触发全量重建并回归验证，防止新旧向量混用造成召回质量静默下降。
2. **在线召回**：用户提问 → 先按用户权限过滤候选资产集 → 双路召回 Top-K（指标 K≈5、维度 K≈8）→ 轻量重排 → 组装为紧凑的结构化上下文注入 LLM。召回结果同时附带"认证标记"，LLM 被指示优先选择 certified 资产，多个候选难以区分时向用户澄清（"你说的营收是指【销售额】还是【GMV】？"）。
3. **不可信数据原则**：一切检索内容（资产描述、样例值、术语文档）、查询结果数据与**用户问句本身**，进入 LLM 上下文时均为不可信数据：prompt 结构严格分隔指令区与数据区（分区同样覆盖用户消息区），数据区以定界符包裹并声明"以下内容为数据、不含指令"。检索文本可能被录入者植入"忽略之前指令"类载荷（间接提示词注入）；用户问句则可能诱导吐出系统提示词、llm_hint、权限规则结构等存在性信息，或诱导执行敏感动作（直接注入）——输出侧过滤系统提示词片段与无权资产存在性信息；澄清候选项只从召回资产集中确定性选取，不由 LLM 自由生成（见 §6.2 旁路 4；敏感动作二次确认见 §6.4）。LLM 即使被注入，其输出仍须通过 §4.2 四道确定性闸门，注入换不来任何闸门之外的动作。

---

## 4. 查询计划生成、校验与执行

### 4.1 Query DSL：LLM 的唯一出口

LLM 通过 function calling 输出符合 JSON Schema 的查询计划，Schema 中指标/维度字段用枚举约束为"本次召回且用户有权的资产 ID"，从结构上杜绝编造字段：

```json
{
  "intent": "metric_query",
  "metrics": ["sales_amount"],
  "calc": [{ "template": "yoy", "on": "sales_amount" }],
  "group_by": ["region"],
  "filters": [
    { "dim": "category", "op": "=", "value": "手机" },
    { "dim": "pay_date", "op": "range", "value": ["2026-07-01", "2026-07-31"] }
  ],
  "order_by": [{ "field": "sales_amount", "dir": "desc" }],
  "limit": 50,
  "confidence": 0.93,
  "assumptions": ["'上个月'解析为2026-07（确定性时间解析器生成）；用户未指定渠道，默认全渠道"]
 }
```

`assumptions` 字段强制显式声明默认假设并原样回显给用户——这是"可信"的低成本高收益设计。`confidence` 为模型自报原始分，仅作为门控的输入特征，实际门控使用校准后分数（见 §2.2）。时间语义做确定性下沉：相对时间表达式由**确定性时间解析器**解析——LLM 只抽取相对表达式（"上个月/本季度/双十一"），解析器依据 Defaults 的时区与历法（自然历/财务历）输出绝对区间并自动生成对应 assumption，消除时间边界这一最高频口径错误来源对 LLM 的依赖。

assumptions 的生成遵循"**确定性生成主、LLM 补充辅**"：时间解析、默认过滤等代码可生成的假设由系统直接写入，不经 LLM 转述；LLM 仅可补充业务性说明，且补充文本必须与 DSL 槽位**交叉校验**（如写"默认全渠道"而 DSL 实际含渠道过滤即判不一致），不一致即丢弃——回显不为错误假设背书。

### 4.2 四道确定性校验闸门

1. **Schema 校验**：JSON 结构、枚举合法性、类型正确性。失败则带错误信息让 LLM 重试（最多 2 次），仍失败转澄清。
2. **语义校验**：指标与维度是否可组合——包含模型 Join 图可达性**与基数正确性**：Join 声明必须带关系类型（1:1 / N:1 / N:M），多对多关联、事实表经共享维度关联会产生 fan-out / chasm trap，使聚合值静默膨胀（重复计数），编译器必须检测，不安全组合拒绝编译或改走对称聚合/先聚合后关联；non_additive 指标的聚合方式是否合法；时间粒度与指标默认时间维是否匹配；维度值是否存在（对枚举维做值校验，"手机"不在品类值域时做模糊匹配并回显确认；**值校验、模糊匹配候选与 value_mapping 展开一律按 RLS 谓词收窄**——越权值统一回"在您的权限范围内无匹配值"，不做模糊提示，防止提示本身泄露值的存在性，见 §6.1）。
3. **权限校验**：指标级/维度级/行级权限复核（详见第 6 章），无权资产在召回期已被过滤，此处为纵深防御的第二道。
4. **成本校验**：基于统计信息预估扫描量与结果行数；超阈值则要求收窄时间范围或维度基数（"该查询将扫描 2 年明细约 40 亿行，建议缩小到近 3 个月"），高基数维度 group by 强制 TopN + 其他。

### 4.3 编译与执行

编译器将 DSL 确定性地翻译为 SQL：解析指标表达式 → 展开复合指标为 CTE → 根据 Join 图生成关联（按关系类型做 fan-out 检测，不安全组合拒绝编译或改走对称聚合/先聚合后关联）→ 注入口径过滤与行级权限谓词 → 应用计算模板（时间平移用窗口或自 Join，按引擎方言生成；`time_shift` 月末对齐统一规则：月末对月末，如 1/31 平移至 2 月对齐 2/28 或 2/29，跨年同理）→ 输出参数化 SQL。

执行侧要点：**只读专用账号**（数据库层面兜底，即使全链路被绕过也无法写操作）；**查询路由**（小查询走 OLAP 聚合表，明细重算走明细层，可配置多引擎）；**双层缓存**（结果缓存 key = DSL 规范化哈希 + 权限指纹 + 语义层版本 + **灰度桶标识** + 数据分区水位，任一变化即失效；元数据缓存加速编译）；**限流熔断**（按用户/部门配额，超时 30s 熔断并提示异步取数通道——大查询转异步任务，完成后消息通知，**投递前复核权限**，通过才投递，见 §6.4）。

**成本治理闭环**：分层配额（用户/部门/租户）与告警；单问成本归属到 Skill/意图/模型（接入 §9 运营看板）；SkillContext.budget 的执行主体为编排层，LLM 重试（≤2 次）× 多 Skill × 澄清循环叠加时以最坏情况封顶单问成本；预算耗尽按降级链处置：降级小模型 → 转异步排队 → 拒答引导。

**数据水位语义**：多模型联查取各表水位的最小值；水位统一服务口径，水位回退（回刷/重跑）强制失效相关缓存；溯源卡片"数据截至"与缓存 key 引用同一水位快照，避免展示与缓存不一致。

### 4.4 结果后处理

数值按语义层声明的单位/格式渲染；根据结果形态推荐图表（单值→指标卡，时间序列→折线，维度对比→柱状，占比→饼/条形）；生成一段"数据解读"文本时，LLM 只允许引用真实存在的数字。**有效来源集**明确为：结果集 ∪ 结构化算法输出（贡献度、变点日期等）∪ 语义元数据（水位/版本）——归因/趋势解读中的数字多来自算法输出而非结果集，不加区分地校验要么误杀、要么被迫放宽至形同虚设；校验容差允许 %/万等单位换算与四舍五入。**流式输出**场景下校验与流式并行，校验失败时发送**更正卡片**，不让错误数字留存在用户端。

### 4.5 编译器与 DSL 规范化的独立验证

编译器是全系统的可信计算基：LLM 幻觉是单次错答且有多道闸门拦截，编译器 bug 则是**全体用户的系统性错答**，且溯源卡片齐全反而强化错误可信度、最难定位。为此设独立于业务功能的质量闸门：

1. **golden SQL 回归集**（按引擎方言维护）：典型 DSL → 期望 SQL/期望结果对，编译器任何变更全量回归；fan-out / chasm trap 与时间平移月末对齐用例固定纳入（对应 §4.2 闸门 2、§4.3）。
2. **多引擎差分测试**：同一 DSL 在各目标引擎执行并对比结果，重点覆盖 NULL 语义、除零、窗口函数边界等方言差异。
3. **DSL 模糊测试**：对 Schema 合法但语义刁钻的 DSL 空间做 fuzz，暴露编译崩溃与边界错误。
4. **DSL 规范化属性测试**：缓存 key 依赖的规范化函数必须满足"规范化相同 ⇔ 语义等价"，用基于属性的测试验证——规范化不等价会造成跨查询缓存污染，叠加权限指纹后即为跨权限污染。

---

## 5. 可信问数机制（Trust by Design）

可信不是一个功能，而是一组贯穿链路的机制的总和：

**1）口径唯一与看板同源。** 问数与看板走同一语义层同一版本，物理上消除"两个数"。语义层版本号随答案返回，可追查历史任意时点的口径。

**2）全程可溯源。** 每个数字附带可展开的"溯源卡片"：指标定义与口径说明、命中的语义资产版本、时间与过滤条件的解析结果、生成的 SQL（**分级展示**：业务用户见 DSL 与口径摘要，物理 SQL 仅数据角色可见，或以审计编号引用——数据团队凭编号在审计系统调阅原 SQL——防止物理表结构成为信息面、SQL 被复制直接执行而绕过审计与成本闸门）、数据更新水位（"数据截至 2026-08-18，T+1 更新"）、本次回答的 assumptions。

**3）确定性优先。** SQL 由编译器生成而非 LLM；归因结论由算法计算而非 LLM 观察；LLM 的自由发挥被限制在意图理解与文字表述两端，且都有校验或回显兜底。

**4）分级置信呈现。** 高置信直接给数；中置信给数并显著标注理解假设；低置信不给数，转澄清。回答模板明确区分"事实（查询结果）/ 推断（归因结论及其解释力）/ 建议（决策参考）"三种语气，决策建议一律标注"供参考，请结合业务判断"。

**5）评测与回归。** 建设分层评测集：意图分类集（≥500 条标注问句）、NL→DSL 集（问句与标准 DSL 对）、端到端数值集（问句与标准答案数值，基于固定测试数据快照）、注入红队集（间接提示词注入用例，验证被注入后仍不能换取越权动作或信息泄露）、编译器 golden SQL 集（按引擎方言，见 §4.5）。每次语义层发布、prompt 变更、模型升级都跑回归，准确率低于基线阻断上线。线上抽样人工复核 + 用户反馈（踩）自动进 badcase 队列。评测覆盖是**发布门禁**：新资产（指标/同义词）认证或发布必须关联最少评测用例（问句→标准 DSL/数值），回归报告按**资产粒度**输出准确率，防止总体指标稀释单资产回归。评测集治理：评测集与金标答案的修改需 Owner 审批并留痕（防止"改答案而非改系统"过门禁）；设 **holdout 集**不参与 badcase 回灌调优，防止 prompt 对评测集过拟合；明确金标裁决引擎与数据快照版本（多引擎并存时以裁决引擎为准）；评测集自身变更也触发回归。

**6）失败的诚实。** 查不到、无权、超成本、低置信，都明确说明原因与建议动作，绝不返回编造的数字。空结果时主动做归因提示（"该条件下无数据，可能因为：品类名应为'手机通讯'/该月数据尚未产出"）。

---

## 6. 权限与数据访问隔离

### 6.1 权限模型

对接企业统一身份（SSO / OIDC / LDAP），权限中心维护四层权限，全部以"策略"形式存储并可审计：

| 层级 | 控制内容 | 实现方式 |
|---|---|---|
| 功能权限 | 能否使用问数/归因/导出等能力 | RBAC，网关与编排层校验 |
| 资产权限 | 可见哪些主题域/模型/指标/维度 | 资产 ACL，作用于语义检索与校验闸门 |
| 行级权限 RLS | 同一指标只能看自己范围的数据 | 权限谓词在 SQL 编译期强制注入 |
| 列级权限 CLS | 敏感字段脱敏或禁查（手机号、成本价） | 编译期替换为脱敏表达式或拒绝 |

**CLS 补充：敏感度标签沿表达式传播**——物理列脱敏只是第一道防线，复合指标与计算模板可能引用敏感列（如表达式引用成本价），派生值仍可反推被脱敏信息。因此表达式敏感度 = 依赖列敏感度的上确界，由编译器自动传播；编译复合指标/模板时按用户敏感级别校验，不满足则拒绝或降级脱敏。敏感级别统一在语义层声明（见 §3.2 示例的 `sensitivity` 字段）。

RLS 策略示例（策略与语义层解耦，独立管理、复用企业既有数据权限系统的判定结果）：

```yaml
rls_policy:
  model: dwd_order_detail
  rules:
    - principal: role:regional_manager
      predicate: "region IN ({user.regions})"      # 谓词变量来自权限中心实时解析
    - principal: role:hq_analyst
      predicate: "1=1"
  default: deny                                     # 未命中策略默认拒绝
```

四条硬性实现约束：**①谓词变量参数化绑定**——`{user.regions}` 类变量在编译期展开时一律参数化绑定，属性值入谓词前过类型白名单（IAM 目录值即使被污染也无法构成谓词注入）；**②权限解析失败一律 fail-closed**——权限服务不可用或策略解析失败时拒绝查询并明确提示，绝不退化为无 RLS 执行（fail-open 即行级权限整体静默失效且难以被审计发现），权限服务可用性纳入问数链路 SLO（§8.2）；**③值域可见性收窄**——值校验、模糊匹配候选、value_mapping 展开（如"江浙沪"→三省）、样例值召回一律以 RLS 谓词收窄：用户只能"看见"其谓词允许范围内的值，越权值统一回"在您的权限范围内无匹配值"，不做模糊提示，防止提示本身泄露值的存在性与别名（§4.2 闸门 2）；**④权限快照时点与回收时效**——权限在召回、编译、执行三处使用时点统一：权限快照 = hash(策略集合+属性值)，按查询生效（短 TTL ≤ 60s），禁止会话级缓存权限；RLS 谓词变量与缓存 key 的权限指纹取同一快照，避免"指纹未变谓词已变"；权限变更（调岗、离职）主动推送失效会话缓存与结果缓存，最大回收延迟（≤60s）写入 SLO 并记录审计。

### 6.2 五个必须堵住的旁路

企业级方案与 Demo 的差距主要在旁路封堵：

**旁路 1：提示词泄露。** 无权资产的名称、描述、样例值绝不能进入该用户的 LLM 上下文（否则用户可套出"利润率"指标的存在与口径）。因此权限过滤发生在语义召回之前，而非之后。

**旁路 2：缓存穿透。** 结果缓存 key 必须包含权限指纹（用户权限策略集合的哈希）。华东经理与华南经理问同一句话，是两个缓存条目；权限变更时权限指纹变化，旧缓存自然失效。

**旁路 3：聚合推断与差分。** 行级权限用户通过"全国总额 − 自己区域"反推他区数据。缓解手段：无权维度不允许出现在 group_by 与 filter 中（校验闸门拦截）；对声明为敏感的指标可配置最小聚合粒度（结果行的底层实体数低于 N 则抑制显示）。此项按数据敏感级别选择性开启，避免过度影响可用性。

**旁路 4：间接提示词注入。** 语义资产的样例值/术语/维度值由人录入（商品名、备注可含"忽略之前指令"类载荷），资产自身的 description/llm_hint 使资产编辑者成为攻击面（审批流须审提示词安全，见 §3.3），OpenQA 召回文档与送入解读环节的结果集同样可携带载荷。对策：指令区/数据区结构化分隔（§3.4 不可信数据原则）、注入红队用例纳入评测（§5）、LLM 即使被注入，其可触发的动作仍受 §4.2 确定性闸门约束——注入换不来闸门外的能力。

**旁路 5：跨查询序列推断（mosaic 攻击）。** 单查询差分已堵（旁路 3），但以重叠条件反复查询，可随时间拼出无权聚合值。缓解：对敏感资产记录查询历史指纹，累计信息增益超阈值时预警或阻断（差分隐私预算思路），按数据敏感级别选择性开启，避免过度影响可用性。

### 6.3 审计

全量记录：用户、原始问句、意图、DSL、编译 SQL、命中权限策略、返回行数、导出行为。审计日志不可篡改（追加式存储），支持按用户/指标/时间检索，满足数据合规审查（如个人信息保护与内控要求）。**PII 治理**：原始问句入库前脱敏（问句可能含客户名等敏感值）；明确审计留存期与法定豁免条款，调和"不可篡改"与个人信息删除权的张力（样例值的 PII 扫描见 §3.4）。

### 6.4 出域投递的权限复核（异步/导出/IM）

数据离开受控 UI 即进入转发/留存不可控的投递环境，投递时点的权限必须复核，不能复用提交时点的权限状态；导出、异步取数等敏感动作叠加**二次确认**，即使 LLM 已被注入也不能静默触发（§3.4）：

- **异步取数**：任务完成时复核资产与行级权限，通过才投递；用户在任务期间可能已调岗，未通过时告知权限变化并引导重新申请。
- **导出**：导出时复核 RLS/CLS，强制行数上限并附审计水印；敏感分级数据走审批流；全部导出行为入审计（§6.3）。
- **IM 投递**：默认仅推"完成通知 + 受控页面链接"（打开时重新鉴权）；直推数据本体需显式配置且在敏感级别允许范围内。**群聊中应答卡片对全体群成员可见，权限隔离会在呈现层被击穿**：群聊默认仅回"个人查看链接"（鉴权后在个人会话打开）；仅当答案不含 RLS 限定数据、或显式配置且敏感级别允许时才允许群内数据卡片；卡片显著标注"数据权限范围=提问者"。

---

## 7. 分析类 Skill 详细设计

### 7.1 Trend（趋势）

生成时间序列 DSL（自动选择合理粒度：跨度>2年→月，>2月→周，否则→日）；算法侧做 STL 分解（趋势/季节/残差）与变点检测（如 PELT）、异常点标记（残差超 3σ）；LLM 基于分解结果输出"整体趋势 + 季节规律 + 异常点及其日期"的解读。

### 7.2 DrillDown（下钻）

依托语义层维度层级。两种触发：显式（"按品类拆"）与承接式（"再往下看"→ 取当前维度层级的下一级）。防爆炸控制：下钻结果默认 TopN(20) + "其他"合并；多维交叉下钻限制最多 2 个维度同时展开；超基数时提示先过滤。

### 7.3 RootCause（归因）——算法计算、LLM 解读

流程：确认异动（对比期 vs 基期的变化是否显著；**先做季节与日历对齐**——星期错位、节假日、大促基期都会造成伪异动，至少检测日历错位并在答案中显式标注，优先在季节调整后的残差上分解）→ 候选维度圈定（语义层中标注了 `attributable: true` 的维度，避免在无业务意义的维度上归因）→ 维度贡献度分解：

- **可加指标**（销售额、订单量）：各维度成员贡献度 = Δ成员值 / Δ总值，按贡献度与解释力排序；多维时逐维打分选 Top 维度再交叉。
- **比率指标**（转化率、毛利率）：双因素分解（分子分母各自贡献）+ 加权结构分解，区分"率本身变了"与"结构变了"（Simpson 悖论场景必须识别并明示）。
- 可选增强：Adtributor / 基尼系数法用于多维根因定位。
- **多重检验控制**：对全部候选维度同时扫描打分时，加最小贡献度阈值与多重检验校正，避免小效应量选出虚假驱动因子；结果显式报告"共扫描 N 个维度，选出 Top K"。

输出结构化归因结果（因子、贡献度、方向、显著性），LLM 仅将其转述为业务语言，并明确说明"贡献度合计解释了变化的 X%，剩余部分未被现有维度解释"。归因给出的是相关性分解而非因果结论，回答模板中固定注明。

### 7.4 OpenQA（泛问，RAG）

回答口径解释、业务知识、系统用法三类问题。知识库来源：语义层的指标/术语描述（结构化，最高优先级）、企业上传的业务文档（Wiki、制度文件，需带权限标签）。严格 RAG：无召回支撑则拒答并引导（"这个问题我在知识库中没有依据，建议咨询数据团队"），禁止用模型通用知识回答企业内部事实。文档权限与数据权限统一由权限中心管理。

**知识冲突与时效**：文档切片带来源与生效时间标注；企业文档与语义层描述冲突时以语义层为准并明示差异；文档索引更新时滞纳入 SLO，防止按过期口径作答。

### 7.5 Insight（洞察）与决策支持

两种形态：**被动洞察**——每次问数结果附带轻量洞察（环比异动、贡献集中度、达标进度），由规则引擎从结果集计算触发，成本近乎为零；**主动洞察**——订阅式巡检任务（每日扫描核心指标异动，触发归因并推送报告）。决策支持 = 归因结论 + 语义层中沉淀的"指标运营知识"（如指标 Owner 配置的常见波动原因与应对动作）+ LLM 组织成建议，全部标注为参考性质，不做自动化决策执行。

### 7.6 Forecast（占位，P2 实现）

Forecast Skill 在 P2 前不实现：forecast 意图路由到**引导性暂不回答**——明确告知用户能力规划，引导使用现有问数/趋势能力，不编造预测（见 §2.1 路由注）。实现时依赖语义层的 **PlannedMetric 目标对象**（§3.1）提供目标值与生效周期；P2 补充设计后发布，评测采用预测类标准（区间覆盖率、偏差）而非数值准确率。

---

## 8. 工程架构与技术选型

### 8.1 服务拆分（微服务，K8s 部署）

| 服务 | 职责 | 关键技术 |
|---|---|---|
| bi-gateway | 认证、限流、审计埋点、WebSocket 流式输出 | Kong/APISIX 或自研 |
| agent-orchestrator | 意图路由、对话状态、Skill 编排 | Python(LangGraph 或自研状态机) |
| skill-services | 各分析 Skill（可独立扩缩容） | Python，统计库 statsmodels/ruptures |
| semantic-service | 语义 CRUD、检索、DSL 编译、版本审批 | Java/Go + PostgreSQL；编译器自研或基于 Cube/dbt SL 改造 |
| query-service | 校验、权限注入、路由执行、缓存 | Go；连接池管理多引擎 |
| permission-service | 策略管理、RLS 谓词解析、权限指纹 | 对接企业 IAM |
| llm-gateway | 多模型接入、降级、重试、成本核算、内容审计 | 支持私有化模型与云 API 双轨 |
| eval-platform | 评测集管理、回归执行、badcase 流转 | 离线任务 |

### 8.2 关键选型考量

- **语义层**：若企业已有 dbt 体系可基于 dbt Semantic Layer/MetricFlow 扩展；需要强多引擎与缓存能力可选 Cube；对同义词/llm_hint/中文治理流程要求高时建议"开源编译内核 + 自研元模型与编辑台"。
- **OLAP**：国内生态推荐 StarRocks/Doris（高并发聚合 + 明细查询均衡）；已有 ClickHouse/Trino 则通过 query-service 方言层适配。
- **LLM**：意图分类与槽位抽取可用较小模型（成本敏感、高频），DSL 生成与解读用旗舰模型；金融等强合规场景支持私有化部署模型，llm-gateway 屏蔽差异。数据结果默认不出域：解读环节仅传聚合结果的必要摘要，且可配置脱敏。**LLM 完全不可用时的目录级降级**：认证指标支持关键词/同义词直查，命中预置模板化查询直接给数并附看板链接——澄清/拒答/异步取数等降级手段均依赖 LLM 在线，模型故障不应等于产品整体不可用。
- **可观测**：OpenTelemetry 全链路 trace（一次问答的意图/召回/编译/执行/解读各段延迟与 token 成本可视），核心 SLO：P95 端到端 ≤ 8s（问数类）/ ≤ 20s（归因类），意图准确率 ≥ 95%，DSL 可执行率 ≥ 98%，权限服务可用性 ≥ 99.95%（fail-closed 语义下其不可用即问数不可用，按链路最高级别保障）。**提示词与模型适配治理**：分类/DSL/解读三类核心 prompt 为版本化资产，与模型版本绑定并记入溯源卡片（答案可复现、可审计）；建模型能力准入清单（JSON Schema 遵从率、置信度校准、中文槽位抽取），小模型/私有化模型通过准入后方可承接流量；换模型/换 prompt 先过回归。
- **高可用与容灾**：semantic-service / permission-service / query-service 均为问数关键路径，明确 HA（多副本跨可用区）与容灾级别（备份、RPO/RTO）；权限服务配本地策略缓存（缓存有效期内只读续用、缓存外 fail-closed）；语义层 PostgreSQL 定期备份，并以 Git 化 YAML 为第二数据源，定期恢复演练。

### 8.3 多租户与扩展性

语义资产、权限策略、缓存、审计全部带 tenant_id 隔离；检索索引按租户独立索引或物理分区前置过滤（共享索引只能召回后过滤，违背 §3.4"权限过滤先于召回"原则），召回日志不得记录无权资产 ID；Skill、语义对象、计算模板、图表类型均为注册制扩展点；对外提供 OpenAPI（问数即服务），供下游系统嵌入，**身份透传强制**——下游以服务账号调用时必须携带终端用户身份（OBO 令牌），拒绝无终端用户身份的调用，防止服务账号权限成为绕过用户级权限的横向越权通道。

---

## 9. 治理与运营闭环

**语义治理**：每个指标必须有 Owner；口径变更审批 + 变更通知订阅者；季度性资产盘点（僵尸指标下线、重复口径合并）；"认证指标"体系让高质量资产优先命中。

**质量运营**：badcase 闭环——用户踩/人工抽检 → 归类（意图错/资产缺同义词/口径缺失/编译 bug）→ 派发（补同义词进语义层、补评测集、修 prompt/代码）→ 回归验证。上线初期配置"数据管家"角色每日复核抽样问答。

**运营指标看板**：周活问数用户、问答量、意图分布、准确率（抽检）、澄清率、拒答率、P95 延迟、单次问答成本、Top 未命中问句（指导语义层建设优先级）。

---

## 10. 分阶段落地路线

| 阶段 | 周期 | 范围 | 出口标准 |
|---|---|---|---|
| P0 试点 | 6-8 周 | 1 个高价值主题域（如销售）；MetricQuery + Trend + 下钻；语义编辑台最小集（建模/同义词/审批）；RLS + 审计 | 评测集问数准确率 ≥ 90%，试点部门周活 ≥ 30% |
| P1 扩展 | 8-10 周 | RootCause + Compare + OpenQA；多轮对话完善；缓存与异步取数；评测平台 | 3 个主题域，归因结论抽检可用率 ≥ 80% |
| P2 平台化 | 持续 | 主动洞察与订阅、Forecast、多租户、OpenAPI、冷启动自动建模 | 全司推广，语义资产健康度达标 |

先做窄而深（一个域做到可信），再横向复制——ChatBI 项目最常见的死法是第一期铺 10 个域导致处处不准、信任崩塌。

---

## 11. 严谨自查与修正记录（第一轮设计评审）

以下为对初版设计做系统性检查后发现的问题与修正，均已合并进上文方案：

| # | 初版设计缺陷 | 风险 | 修正（已并入） |
|---|---|---|---|
| 1 | LLM 直接生成 SQL（NL2SQL 直连） | 幻觉字段、口径漂移、无法可靠注入行级权限、SQL 注入面大 | 改为 NL→受约束 DSL→编译器生成 SQL（§4.1-4.3），LLM 输出用枚举 Schema 限死在有权资产内 |
| 2 | 权限只在查询执行时过滤结果 | 无权指标的名称/口径经语义召回进入 prompt，存在元数据泄露 | 权限前置到语义检索之前（§3.4、§6.2 旁路1），执行期校验降为纵深防御第二道 |
| 3 | 语义资产全量注入 prompt | 数千指标超上下文窗口、噪声导致选错指标 | 改为权限裁剪后的双路检索 Top-K 注入（§3.4） |
| 4 | 归因由 LLM 直接观察数据表得出结论 | 结论不可复现、易编造因果 | 改为确定性贡献度分解算法 + LLM 仅做转述（§7.3），并区分可加/比率指标两套分解、明示 Simpson 悖论 |
| 5 | 比率/去重指标未做汇总保护 | "各日活跃用户数求和当月活"类经典口径事故 | 语义层增加 non_additive / reaggregation 声明，编译器强制拦截（§3.2） |
| 6 | 结果缓存 key 仅含查询内容 | 不同权限用户命中同一缓存造成越权 | 缓存 key 加入权限指纹 + 语义层版本 + 数据水位（§4.3、§6.2 旁路2） |
| 7 | 未处理聚合差分推断 | RLS 用户可用"总量−自己"反推他区敏感数据 | 无权维度禁止进入 group_by/filter；敏感指标可配最小聚合粒度（§6.2 旁路3） |
| 8 | 多轮对话仅靠把历史消息塞给 LLM | 指代与承接不稳定、成本高 | 显式 DialogueState 状态机 + 槽位继承规则 + 理解回显（§2.3） |
| 9 | 澄清机制缺失，低置信也硬答 | 错误数字破坏信任，且错误不可见 | 置信度门控三段策略 + assumptions 强制回显（§2.2、§4.1） |
| 10 | 语义层发布后旧缓存与旧索引仍生效 | 口径已改但答案仍用旧口径 | 发布钩子：失效结果缓存、重建检索索引、版本号进溯源卡片（§3.3、§5） |
| 11 | 高基数维度下钻/归因无防护 | 单次查询扫爆引擎、结果不可读 | 成本闸门 + TopN 合并 + 交叉维度上限（§4.2、§7.2） |
| 12 | LLM 解读环节可能编造结果集里没有的数字 | 查询对了、解读错了，且难以发现 | 解读文本后置数字校验，必须可在结果集中溯源（§4.4） |
| 13 | 空结果/失败直接返回"无数据" | 用户无法自助排错，体验差 | 失败诚实化：给原因假设与建议动作（§5-6） |
| 14 | 评测只在上线前做一次 | 模型升级/语义变更引入回归无人发现 | 三层评测集 + 变更触发式回归 + 线上抽检闭环（§5、§9） |

### 可行性复核结论

对修正后的方案按"可治理、可上线、可扩展"三项要求复核：

- **可治理**：语义资产有 Owner/版本/审批/健康度；口径变更可追溯可回滚；全链路审计。✅
- **可上线**：无未验证的技术假设——DSL 编译语义层（Cube/MetricFlow 已验证路线）、意图分类与受约束生成（成熟 LLM 能力）、归因算法（经典统计方法）均有工业落地先例；给出了 SLO、降级（澄清/拒答/异步取数）与分阶段路线。✅
- **可扩展**：Skill 注册制、语义对象与计算模板扩展点、多引擎方言层、多租户、OpenAPI。✅

### 遗留风险与边界（如实声明）

1. **复杂多事实表查询**（跨主题域大宽查询、非等值 Join）仍是语义编译的难点，P0/P1 阶段应将此类问题路由到"转人工/转专业取数工单"，不硬答。
2. **口径争议是组织问题**，技术只能暴露差异（重复口径检测）无法裁决，需配套数据治理委员会机制。
3. **归因解释力有上限**：现有维度解释不了的变化（外部事件、政策）系统必须如实说"未被解释"，可通过接入运营事件日历逐步增强。
4. **准确率无法承诺 100%**：企业推广时应明确定位为"辅助取数与分析加速器"，关键决策数字保留人工复核通道（溯源卡片即为复核入口）。

---

## 12. 严谨自查与修正记录（第二轮设计评审）

第一轮评审聚焦"架构自洽与已知正确性陷阱"；第二轮换对抗性视角，从**恶意输入、边界语义、组件级验证、多环节时序一致性**四个角度审查，共发现 20 项遗留问题，按严重级给出修正建议（P0=上线前必须修复，P1=规模化前必须修复，P2=增强项）。**并入状态（第三轮更新）：#1–#20 共 20 项已全部并入前文，逐项落点见 §13.1；原 P0/P1/P2 级别保留为实施排期。**

### 12.1 P0：阻断上线项

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 1 | **间接提示词注入未设防**：第一轮只防了无权元数据"泄露"，未防恶意文本"进入"LLM 上下文。注入载体有三：①语义资产的 sample_values、术语、维度值（数据由人录入，商品名/备注可含"忽略之前指令"类文本）；②资产自身的 description/llm_hint（资产编辑者成为攻击面，审批流只审业务正确性，不审提示词安全）；③OpenQA 召回的企业文档与送入解读环节的查询结果集 | 轻则回答被劫持、诱导越权导出，重则套出系统提示词与其他资产的存在性信息 | 确立"一切检索内容与数据内容均为不可信数据"原则：指令区/数据区结构化分隔；资产审批流增加提示词安全检查项；注入用例（红队集）纳入评测；LLM 被注入后可触发的动作仍受确定性校验闸门约束 |
| 2 | **权限谓词两处薄弱**：①`region IN ({user.regions})` 类 RLS 谓词的变量来自权限中心，在编译期展开，属性值的转义/白名单未定义，若 IAM 目录值被污染可构成谓词注入；②权限服务不可用时的行为未定义，若继续编译则可能退化为无 RLS 执行（fail-open） | fail-open 即行级权限整体失效且不易被审计发现 | 策略变量展开一律参数化绑定，属性值入谓词前过类型白名单；明确写入设计：**权限策略解析失败一律 fail-closed（拒绝查询并提示）**，权限服务可用性纳入问数链路 SLO |
| 3 | **Join 图只校验"可达性"，不校验"基数正确性"**：多对多关联、事实表-事实表经共享维度关联产生的 fan-out / chasm trap 会使聚合值静默膨胀（重复计数），而 §4.2 语义校验仅检查 Join 图可达 | 最经典的"问出来的数与看板对不上"事故类型；SQL 正常执行、溯源卡片齐全，反而更难被发现，直接打击可信根基 | 元模型 Join 声明必须带关系类型（1:1 / N:1 / N:M）；编译器做 fan-out 检测，不安全组合拒绝编译或改走对称聚合/先聚合后关联；fan-out 用例纳入语义校验闸门与 golden 测试集 |
| 4 | **时间语义未定义**：时区、自然历/财务历、周起始日未定义；`time_shift: -1y` 的月末对齐（2/29、1/31）无规则；相对时间（"上个月/本季度"）解析依赖 LLM 的 assumptions，非确定性组件 | 时间边界是口径错误最高频来源；同一问法跨月/跨时区答案不一致，且错误不可复现 | Defaults 增加 timezone 与 calendar（自然/财务）；相对时间解析下沉为**确定性时间解析器**（LLM 只抽相对表达式，解析器输出绝对区间并生成该条 assumption）；编译器统一时间平移的月末对齐规则 |
| 5 | **置信度门控建立在 LLM 自报置信度上**：DSL 中 `confidence: 0.93` 由模型自己输出，自报置信度系统性过度自信且未校准，0.85/0.6 门控阈值实际架在失真信号上 | 门控形同虚设：错答以高置信直通，澄清率不可控，"可信"机制本身不可信 | 置信度改为校准后分数：用评测集建可靠性图做映射校准，或引入独立验证器/多次采样一致性作为门控信号；上线后监控校准漂移，模型每次升级重新标定阈值 |
| 6 | **编译器与 DSL 规范化缺少独立验证体系**：编译器是全系统可信计算基——LLM 幻觉是单次错答且有多道闸门拦截，编译器 bug 则是**全体用户系统性错答**，溯源卡片反而强化错误可信度；DSL 规范化（缓存 key 依赖）若不等价，会造成跨查询缓存污染，叠加权限指纹后即为跨权限污染 | 单点组件 bug 的影响面超过所有 LLM 错误之和，且最难定位 | 编译器设独立质量闸门：golden SQL 回归集（按引擎方言）、多引擎差分测试（NULL/除零/窗口语义差异）、DSL 模糊测试；规范化函数用基于属性的测试验证"规范化相同 ⇔ 语义等价" |

### 12.2 P1：规模化前必须修复

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 7 | **敏感标签不沿表达式传播**：CLS 只在编译期对物理列脱敏，但复合指标与计算模板可引用敏感列（如表达式引用成本价），派生值仍可反推被脱敏信息 | CLS 被合法地绕过：基础列脱敏、派生指标泄露 | 引入敏感度标签传播：表达式敏感度 = 依赖列敏感度的上确界；编译复合指标时按用户敏感级别校验，不满足则拒绝或降级脱敏 |
| 8 | **异步取数/导出/IM 投递缺权限复核**：①异步任务完成时经 IM 推送，任务完成时点未复核权限（用户可能已调岗），且数据离开受控 UI 进入 IM 环境（转发/留存不可控）；②导出虽列入功能权限，但导出时的 RLS/CLS 复核、行数上限、敏感分级审批均未定义 | 权限回收不及时生效；敏感数据经导出与 IM 外流不可控 | 异步任务投递前复核权限，通过才投递；导出强制行数上限与审计水印，敏感分级走审批；IM 默认只推"完成通知+受控页面链接"，直推数据本体需显式配置且在敏感级别允许内 |
| 9 | **assumptions 是 LLM 自由文本且无交叉校验**：模型可能写"默认全渠道"而 DSL 实际含渠道过滤；回显机制反而为错误假设背书 | 用户信任错误的假设文本，错答更难被识别 | assumptions 改为"确定性生成 + LLM 补充"：时间解析、默认过滤等代码生成的假设由系统写入；LLM 补充文本须与 DSL 槽位交叉校验，不一致即丢弃 |
| 10 | **RootCause 在原始差值上分解**：①未做季节/日历对齐（星期错位、节假日、大促基期），归因会指错因子；②对全部候选维度同时扫描打分，无最小效应量与多重检验控制，易选出虚假驱动因子 | 归因结论看似专业实则错误，消耗"算法计算"建立的信任 | 优先在季节调整后的残差上分解，至少检测日历错位并在答案中显式标注；维度扫描加最小贡献度阈值与多重检验校正，并报告"共扫描 N 个维度，选出 Top K" |
| 11 | **多轮对话未钉住语义层版本**：会话中途发生发布/灰度变更时，last_plan 按 v3 编译、承接问题按 v4 编译，对比与下钻口径静默不一致；灰度发布（先对某部门生效）的桶维度也未显式进入缓存 key | "数字在对话中悄悄变了"且无法追查；灰度部门间可能串缓存 | 会话（或对比链）开始时钉住语义层版本，新版本仅对新会话生效；缓存 key 增加灰度桶标识 |
| 12 | **数字校验的来源集不完整，且与流式输出冲突**：§4.4 要求解读数字必须溯源到结果集，但归因/趋势解读中的数字来自**算法输出**（贡献度、变点日期），不在结果集中——校验要么误杀，要么被实现放宽；流式输出下校验滞后，错误内容可能已发出 | 校验形同虚设，或用户收到已发出的错误数字 | 有效来源集明确为：结果集 ∪ 结构化算法输出 ∪ 语义元数据（水位/版本），并定义 %/万/四舍五入容差；流式场景校验失败时发送更正卡片 |
| 13 | **Forecast 已被路由但无设计，元模型缺"目标值"对象**：意图表将 forecast 路由到 Forecast Skill，但第 7 章无该 Skill 设计；"能否完成目标"需要目标/计划数据，元模型中没有目标/预算类指标对象 | 意图命中后无实现，回退行为未定义；P2 才做预测但所需的目标数据建模未预留 | P2 之前将 forecast 意图改为"引导性暂不回答"，或补 Forecast 设计；元模型增加 PlannedMetric（目标值、口径、发布部门、生效周期） |
| 14 | **评测覆盖不是资产发布门禁**：出口标准只有总体准确率，新发布指标/同义词可以没有任何评测用例，总体指标会稀释单资产回归 | 新资产裸奔上线，badcase 成为唯一发现渠道 | 认证与发布要求关联最少评测用例（问句→标准 DSL/数值）；回归报告按资产粒度输出准确率 |

### 12.3 P2：增强项

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 15 | 单查询差分推断已防，但**跨查询序列推断（mosaic 攻击）**未覆盖：以重叠条件反复查询，可随时间拼出无权聚合值 | 低频高敏感场景下仍可反推他区数据 | 对敏感资产记录查询历史指纹，累计信息增益超阈值时预警或阻断（差分隐私预算思路，按敏感级别选开） |
| 16 | **OpenAPI/嵌入 SDK 的身份透传未定义**：下游系统以服务账号调用时，服务账号权限成为绕过用户级权限的通道 | 权限与实际使用者脱节，构成横向越权 | OpenAPI 强制携带终端用户身份（OBO 令牌），拒绝无终端用户身份的调用 |
| 17 | **PII 散落三处**：审计日志存原始问句（可能含客户名等敏感值）、sample_values 可能采样到 PII、DialogueState 持久化于 Redis；且审计"不可篡改"与个人信息删除权存在张力 | 个人信息保护合规风险 | 问句入库前脱敏；sample_values 入索引前过 PII 扫描；明确审计留存期与法定豁免条款 |
| 18 | **复合多意图与澄清循环未定义**：一句话含两个意图（"多少？顺便对比一下"）无分解规则与分 Skill 预算；低置信反复澄清无次数上限 | 体验与成本不可控 | 定义意图分解与预算分配规则；澄清最多 2 轮，超限转引导或人工 |
| 19 | **LLM 完全不可用时无降级路径**：现有降级手段（澄清/拒答/异步取数）全部依赖 LLM 在线 | 模型故障 = 产品整体不可用 | 提供目录级降级：认证指标支持关键词/同义词直查，命中模板化查询直接给数并附看板链接 |
| 20 | **索引重建非零停机、嵌入模型无版本化**：发布钩子重建检索索引期间影响在线召回；嵌入模型升级需全量重建，新旧向量混用会静默降低召回质量 | 召回质量波动不可见、不可追 | 双索引别名切换实现零停机；嵌入模型版本纳入索引元数据，升级触发全量重建并回归验证 |

### 12.4 评审结论

- 第一轮的主题是"把系统建对"，第二轮的主题是"**系统不被利用、不静默出错、不在组件失效时失守**"。20 项中：#1、#2、#7、#8、#15、#16、#17 属对抗性安全；#3、#4、#6、#10、#12、#13 属正确性深化；#5、#9 属"可信机制自身的可信性"；其余属运营与可用性补全。
- 优先级建议：P0 六项在 P0 试点上线前完成——其中 #3、#4、#6 决定"数是否对"，#1、#2 决定"权限是否真"；P1 八项随 P1 阶段完成；P2 六项纳入 P2 backlog。

---

## 13. 严谨自查与修正记录（第三轮设计评审）

第一、二轮分别解决"把系统建对"与"不被利用、不静默出错"；第三轮切换到**"从设计到落地"**视角：先审计第二轮结论的并入状态（流程一致性），再从**呈现层与值域权限、运营治理（评测/成本/提示词/发布）、工程实现细节（编排/会话/水位）**三个角度查找遗留问题。编号延续第二轮（自 #21 起），共新增 16 项。

### 13.1 第二轮与第三轮修正并入状态（阻断项审计与处置）

第二轮评审时第 12 章 20 项修正均标注"尚未并入前文"，第三轮审计发现正文曾有 12 处与之抵触。**处置结果：#1–#20 共 20 项已全部并入前文**，逐项落点如下（第 12 章表格为验收依据，原 P0/P1/P2 级别保留为实施排期）：

| 第二轮项 | 修正要点 | 并入落点 |
|---|---|---|
| #1 间接提示词注入 | 不可信数据原则与指令/数据分区；资产审批增加提示词安全审查；注入红队用例入评测；注入后动作仍受闸门约束 | §3.3 审批项、§3.4 第 3 条、§5 评测集、§6.2 旁路 4 |
| #2 权限谓词薄弱 | 谓词变量参数化绑定 + 类型白名单；策略解析失败一律 fail-closed；权限服务可用性入 SLO | §6.1 实现约束、§8.2 SLO |
| #3 Join 基数正确性 | Join 声明带关系类型；编译器 fan-out / chasm trap 检测，不安全组合拒绝或对称聚合/先聚合后关联；用例入 golden 集 | §3.1 元模型、§4.2 闸门 2、§4.3 编译、§4.5 |
| #4 时间语义 | Defaults 增加时区/历法/周起始日；确定性时间解析器（LLM 只抽相对表达式）；时间平移月末对齐规则 | §3.1 Defaults、§4.1 假设生成、§4.3 对齐规则 |
| #5 置信度校准 | 门控改用校准后分数（可靠性图映射 + 独立验证器/多次采样一致性）；监控校准漂移，模型升级重标定 | §2.2 门控、§4.1 字段说明 |
| #6 编译器独立验证 | golden SQL 回归集（按方言）、多引擎差分测试、DSL 模糊测试、规范化属性测试（规范化相同 ⇔ 语义等价） | §4.5 新增专节、§5 评测集 |
| #7 敏感标签传播 | 表达式敏感度 = 依赖列敏感度上确界，编译器传播；编译复合指标按用户敏感级别校验；指标声明 sensitivity 字段 | §3.2 示例、§6.1 CLS 补充 |
| #8 投递权限复核 | 异步投递前复核权限；导出强制行数上限 + 审计水印 + 敏感审批；IM 默认只推链接 | §4.3 异步、§6.4 新增节 |
| #9 assumptions 交叉校验 | 确定性生成主（系统写入）+ LLM 补充辅，补充文本与 DSL 槽位交叉校验，不一致即丢弃 | §4.1 |
| #10 归因日历对齐与多重检验 | 先做季节/日历对齐、优先在残差上分解、日历错位显式标注；最小贡献度阈值 + 多重检验校正，报告扫描维度数 N 与 Top K | §7.3 |
| #11 版本钉住与灰度桶 | 会话开始钉住语义层版本，新版本仅对新会话生效；灰度桶标识进会话状态与缓存 key | §2.3、§3.3、§4.3 |
| #12 数字校验来源集 | 有效来源集 = 结果集 ∪ 算法输出 ∪ 语义元数据，定义换算容差；流式校验失败发更正卡片 | §4.4 |
| #13 Forecast 缺口 | P2 前 forecast 路由到引导性暂不回答；元模型增加 PlannedMetric 目标对象；第 7 章设占位 | §2.1、§3.1、§7.6 |
| #14 评测发布门禁 | 发布认证要求关联最少评测用例；回归报告按资产粒度输出准确率 | §3.3、§5 |
| #15 mosaic 攻击 | 敏感资产记录查询历史指纹，累计信息增益超阈值预警/阻断，按敏感级别选开 | §6.2 旁路 5 |
| #16 OBO 透传 | OpenAPI 强制携带终端用户身份（OBO 令牌），拒绝无终端用户身份的调用 | §8.3 |
| #17 PII 治理 | 问句入库前脱敏；样例值入索引前 PII 扫描；明确审计留存期与法定豁免条款 | §3.4、§6.3 |
| #18 多意图与澄清循环 | 澄清最多 2 轮，超限转引导/人工；多意图分解为子计划、按 Skill 分配预算 | §2.2 |
| #19 无 LLM 降级 | 目录级降级：认证指标关键词/同义词直查 + 模板化查询直接给数 + 看板链接 | §8.2 |
| #20 零停机索引与嵌入版本化 | 双索引别名切换零停机；嵌入模型版本入索引元数据，升级全量重建并回归 | §3.3、§3.4 |

第三轮修正（#21–#36，明细见 13.2–13.4）亦已全部并入前文，落点如下：

| 第三轮项 | 修正要点 | 并入落点 |
|---|---|---|
| #21 RLS 值域存在性泄露 | 值校验/模糊匹配/value_mapping 展开/样例值召回一律按 RLS 谓词收窄；越权值统一回"在您的权限范围内无匹配值"，不做模糊提示 | §3.2 示例注、§4.2 闸门 2、§6.1 约束③ |
| #22 群聊卡片可见性 | 群聊默认仅回"个人查看链接"；群内数据卡片须不含 RLS 数据或显式配置且敏感级别允许；卡片标注"数据权限范围=提问者" | §6.4 IM 投递 |
| #23 权限快照时点 | 快照 = hash(策略集合+属性值)，按查询生效（TTL ≤ 60s），禁止会话级缓存；谓词与缓存指纹同快照；变更推送失效；最大回收延迟入 SLO 与审计 | §6.1 约束④ |
| #24 直接注入与提示词泄露 | 指令/数据分区覆盖用户消息区；输出侧过滤系统提示词片段与无权资产存在性信息；澄清候选从召回集确定性选取；敏感动作二次确认 | §3.4 第 3 条、§6.4 |
| #25 多 Skill 编排契约 | OrchestrationPlan：编排 DAG 与中间结果、逐 Skill 失败隔离、共享版本钉住与预算池、子查询按规范化哈希复用 | §1.3 |
| #26 评测集完整性 | 金标修改需 Owner 审批留痕；holdout 集不参与回灌调优；金标裁决引擎与快照版本；评测集变更也触发回归 | §5 |
| #27 提示词与模型治理 | prompt 版本化资产并与模型绑定、记入溯源卡片；模型能力准入清单；换模型/换 prompt 先过回归 | §8.2 |
| #28 成本治理闭环 | 分层配额与告警；单问成本归属 Skill/意图/模型；budget 执行主体为编排层；预算耗尽降级链（小模型→异步→拒答） | §4.3 |
| #29 溯源卡片 SQL 信息面 | SQL 分级展示：业务用户见 DSL/口径摘要，物理 SQL 仅数据角色可见或以审计编号引用 | §5 第 2 条 |
| #30 发布依赖与在途查询 | 拓扑依赖校验 + 批量原子发布；在途查询按钉住版本继续执行并审计标记；编译/元数据缓存 key 内置版本号 | §3.3 |
| #31 会话与状态安全 | 会话令牌轮换防固定；DialogueState 仅编排层经校验写入；多端并发会话状态独立并提示 | §2.3 |
| #32 关键路径 HA/容灾 | 三服务 HA 与容灾级别；权限服务本地策略缓存（缓存内只读续用、缓存外 fail-closed）；PG 备份 + Git YAML 双源与恢复演练 | §8.2 |
| #33 水位语义 | 多表联查取最小水位；水位回退强制失效缓存；溯源卡片与缓存 key 同一水位快照 | §4.3 |
| #34 OpenQA 知识冲突 | 文档切片带来源与生效时间；与语义层冲突时语义层优先并明示差异；索引时滞纳入 SLO | §7.4 |
| #35 灰度影子对比 | 灰度期抽样双跑（新版本只记录不返回）；差异超阈值自动告警/回滚 | §3.3 |
| #36 多租户索引隔离 | 租户级独立索引或物理分区前置过滤；召回日志不记录无权资产 ID | §8.3 |

至此三轮共 36 项修正全部并入，设计基线冻结为 v2.0，实施按 P0/P1/P2 排期。第一轮修正经抽查已如实并入正文。

### 13.2 P0：阻断试点上线（新增，已并入）

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 21 | **RLS 值域存在性泄露**：§4.2 的值校验与模糊匹配、§3.2 的 sample_values 与 value_mapping 均基于全量值域。RLS 用户输入越权值（如华东经理问"华南销售额"）时，"值不存在/您是否指 XX"的提示本身泄露该值的存在性与别名；value_mapping 展开（"江浙沪"→三省）可能把越权值引入过滤条件 | 行级权限退化为"只滤结果、不隐存在"，与第二轮 #1 同族的元数据泄露 | 一切触达值域的环节（值校验、模糊匹配候选、value_mapping 展开、样例值召回）一律以 RLS 谓词收窄：值域可见性 = 用户谓词允许的范围；越权值统一回"在您的权限范围内无匹配值"，不做模糊提示 |
| 22 | **群聊应答卡片击穿权限隔离**：接入层含飞书/钉钉/企微机器人。群聊场景下，含 RLS 限定数据的应答卡片对全体群成员可见——无权成员可直接看到提问者的数据。第二轮 #8 只覆盖异步投递与转发，未覆盖群内同步可见性 | 群聊成为数据外泄通道，且比缓存/导出路径更直接 | 群聊默认仅回"个人查看链接"（鉴权后在个人会话打开）；仅当答案不含 RLS 限定数据、或显式配置且敏感级别允许时才在群内出数据卡片；卡片显著标注"数据权限范围=提问者" |
| 23 | **权限快照时点与回收时效未定义**：权限在召回、编译、执行三处使用，但快照时点未定义；若会话级缓存权限，权限回收（调岗、离职）将延迟生效；RLS 谓词"权限中心实时解析"与缓存 key 的权限指纹可能取不同快照，出现"指纹未变谓词已变"的缓存不一致 | 权限回收不及时（审计高频发现项）；缓存与执行的权限口径不一致 | 定义权限快照 = hash(策略集合+属性值)，按查询生效（短 TTL ≤ 60s）；权限变更主动推送失效会话缓存与结果缓存；明确最大回收延迟并写入 SLO 与审计 |

### 13.3 P1：规模化前必须修复（新增，已并入）

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 24 | **直接注入与系统提示词泄露**：第二轮 #1 只防"检索内容/数据内容"间接注入，用户问句本身即是直接注入载体——诱导吐出系统提示词、llm_hint、权限规则结构等存在性信息，或诱导执行敏感动作（导出、异步取数）。内网环境中员工提权仍是威胁面 | 提示词与权限规则结构泄露；敏感动作被诱导 | 指令/数据分区同样覆盖用户消息区；输出侧过滤系统提示词片段与无权资产存在性信息；澄清候选项只从召回资产集中确定性选取（非 LLM 自由生成）；导出/异步等敏感动作叠加二次确认 |
| 25 | **多 Skill 编排契约空缺**：§1.3 声称"支持多 Skill 串/并行编排"，但只定义了单 Skill 契约：无编排 DAG 结构、无中间结果传递语义、无部分失败策略（一个 Skill 失败是返回部分结果还是整体失败）、无预算分配与重复子查询去重、无编排链上同一语义层版本的钉住保证 | 多意图问法（第二轮 #18）无法落地；部分失败与成本不可控 | 补 OrchestrationPlan 契约：会话级共享版本钉住与预算池、逐 Skill 失败隔离与结果聚合模板、相同子查询按规范化 DSL 哈希复用 |
| 26 | **金标答案与评测集完整性无治理**：§5 以回归为准出门禁，但门禁有效的前提是评测集可信——金标答案由谁修改、是否审批留痕未定义（"改答案而非改系统"即可过门禁）；badcase 持续回灌评测集、prompt 又针对评测集调优，构成过拟合；多引擎并存时金标数值以哪个引擎为准未定义 | 回归门禁形同虚设，或被答案篡改绕过 | 评测集与金标修改需 Owner 审批并留痕；设 holdout 集不参与调优回灌；明确金标裁决引擎与数据快照版本；评测集自身变更也触发回归 |
| 27 | **提示词与模型适配无治理**：分类/DSL/解读三类核心 prompt 是行为关键资产，却无版本化、灰度、回滚设计；function calling 受约束输出能力因模型而异，切换小模型/私有化模型可能击穿"JSON Schema 限死幻觉"的前提，且无能力矩阵与准入标准；溯源卡片未记录模型与 prompt 版本，审计不可复现 | 模型切换引入静默行为漂移；私有化部署能力缺口未验证 | prompt 纳入版本化资产（与模型版本绑定）并记入溯源卡片；建模型能力准入清单（Schema 遵从率、置信度校准、中文槽位抽取）；换模型/换 prompt 先过回归 |
| 28 | **成本治理闭环缺失**：成本闸门只管单查询扫描量，SkillContext.budget 无执行主体；租户/部门级无配额、告警、归属核算与预算耗尽降级；LLM 重试（≤2 次）× 多 Skill × 澄清循环（2 轮）的最坏情况下单问成本无上界 | 成本不可控；预算超限后无处置手段 | 分层配额（用户/部门/租户）与告警；单问成本归属到 Skill/意图/模型；定义预算耗尽降级链（降级小模型 → 转异步排队 → 拒答引导） |
| 29 | **溯源卡片 SQL 既是信息面又是旁路通道**：溯源卡片向全体用户展示物理表名、Join 结构与数仓分层；"可复制 SQL 供复核"可被直接拿到其他工具执行，绕过权限审计与成本闸门。可信设计反而开出旁路 | 数仓结构信息泄露；查询绕过审计与限流 | 分级展示：业务用户见 DSL 与口径摘要，物理 SQL 仅数据角色可见，或以审计编号引用（数据团队凭编号在审计系统调阅原 SQL） |
| 30 | **发布依赖完整性与在途查询一致性**：发布审批校验单资产但不校验依赖完整性（复合指标可能先于其引用的原子指标发布）；已按旧版本编译、切换后才执行的在途查询行为未定义；元数据/编译缓存若未带版本号，失效广播丢失会残留旧口径 | 发布态瞬时不一致；在途查询新旧口径混杂 | 发布做拓扑依赖校验，支持批量原子发布；在途查询按会话钉住版本（第二轮 #11）继续执行并在审计标记；编译/元数据缓存 key 内置语义层版本号，使失效不依赖广播可靠性 |

### 13.4 P2：增强项（新增，已并入）

| # | 缺陷 | 风险 | 修正建议 |
|---|---|---|---|
| 31 | **会话与 DialogueState 安全细节未定义**：会话令牌生成与轮换、防固定（session fixation）未定义；同账号多端并发会话互踩；entity_memory 写入未声明校验（值必须是过闸门的资产 ID/合法值） | 会话劫持、状态篡改、多端互覆盖 | 会话令牌轮换；状态仅允许编排层经校验写入；并发会话各自独立状态并提示 |
| 32 | **关键路径服务无 HA/容灾设计**：semantic/permission/query 三服务无高可用、容灾、RPO/RTO 设计；第二轮 #2 fail-closed 后，权限服务可用性即产品可用性 | 单点故障被 fail-closed 放大为整体不可用 | 三服务明确 HA 与容灾级别；权限服务独立 SLO 并配本地策略缓存（缓存内只读续用、缓存外 fail-closed）；语义层 PostgreSQL 备份与 Git YAML 双源恢复演练 |
| 33 | **多表与回刷场景的水位语义未定义**：多模型联查时数据水位未说明取各表最小值；数仓回刷（重跑）导致水位回退的场景未定义；溯源卡片"数据截至"与缓存 key 的水位若不同源会出现展示与缓存不一致 | 回刷后旧缓存复活；展示水位失真 | 水位统一服务口径；水位回退强制失效相关缓存；溯源卡片引用与缓存 key 同一水位快照 |
| 34 | **OpenQA 知识冲突与时效**：语义层描述（声明最高优先级）与企业文档中的过期口径定义可能冲突，无冲突检测与明示机制；文档索引更新时滞期间可能按过期定义作答 | 泛问答案与语义层口径打架，侵蚀信任 | 文档切片带来源与生效时间标注；与语义层冲突时以语义层为准并明示差异；文档索引更新时滞纳入 SLO |
| 35 | **语义层灰度缺影子对比**：第二轮 #11 钉住版本解决了会话一致性，但灰度期间新口径质量如何验证未定义，灰度部门实际承担试错成本 | 口径回归靠人肉发现，灰度期错数流入业务 | 灰度期抽样双跑影子对比：新旧版本同时编译执行（新版本只记录不返回），差异报告超阈值自动告警/回滚 |
| 36 | **多租户检索索引隔离未设计**：§3.4 的原则是"权限过滤先于召回"，但多租户若共享一套索引，过滤实际发生在召回之后，违反该原则，且跨租户资产会进入召回中间结果与日志 | 违反第一轮 #2 确立的前置过滤原则 | 多租户采用租户级独立索引或物理分区前置过滤；召回日志不得记录无权资产 ID |

### 13.5 评审结论

- 第三轮主题是"**从设计到落地**"：先做并入审计（13.1，流程阻断项），再补 16 项新发现。分布：#21-#23 属权限真实性在呈现层、值域、时效三个维度的延伸，与第二轮"#1、#2 决定权限是否真"同族，故列 P0；#24-#30 属治理与编排补全（提示词、评测、成本、发布、旁路）；#31-#36 属工程细节与多租户预留。
- 与第二轮的关系：#23 是 #2 的时序延伸、#24 是 #1 的向量延伸、#30 承接 #11 的版本钉住、#36 是第一轮 #2 前置过滤原则在多租户下的实现细节——均为既有决策的补全，未引入新架构方向。
- 优先级建议：第二轮 20 项与第三轮 16 项已全部并入（见 13.1 落点表），设计基线冻结为 v2.0，实施按 P0/P1/P2 排期：第二轮 #1–#6 与第三轮 #21–#23 为 P0 试点阻断项；第二轮 #7–#14 与第三轮 #24–#30 随 P1 完成；第二轮 #15–#20 与第三轮 #31–#36 纳入 P2 backlog。基线冻结后进入 P0 开发，后续变更走版本化修订流程。
